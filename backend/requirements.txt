# DRF 3.17 supports Django through 6.0. Keep both frameworks inside that
# tested compatibility window instead of allowing pip to select Django 6.1.
Django>=6.1,<6.2
djangorestframework>=3.18.0,<3.19
djangorestframework-simplejwt>=5.5.1
psycopg2-binary>=2.9.12
django-cors-headers>=4.9.0
Pillow>=12.3.0
django-storages>=1.14.6
boto3>=1.43.67
minio>=7.2.20
requests>=2.34.2
httpx>=0.28.1  # ingesta: scraping desde el admin (producción también lo usa)
curl_cffi>=0.16.0  # ingesta/plusvalia: pasa Cloudflare imitando el TLS de Chrome (el admin scrapea en producción)
gunicorn>=26.0.0
celery>=5.6.3  # task queue; the Redis broker is shared with the other Aents systems
redis>=8.1.0
django-redis>=7.0.0  # Django cache backend on the same shared Redis, separate DB index
dj-database-url>=3.1.2
django-allauth>=65.19.0
dj-rest-auth>=7.2.0
google-auth>=2.56.3
google-auth-oauthlib>=1.4.0

# Testing
pytest>=9.1.1
pytest-django>=4.13.0
pytest-cov>=7.1.0
pytest-xdist>=3.8.0  # Para tests paralelos
factory-boy>=3.3.3  # Para crear datos de prueba
faker>=40.36.0  # Para generar datos falsos
