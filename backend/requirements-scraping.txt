# Dependencias SOLO para el scraping en LOCAL (proyecto de ingesta).
# NO se instalan en producción: producción solo corre `ingesta_import`, que
# no scrapea ni descarga de los portales.
#
#   pip install -r requirements-scraping.txt
#
httpx>=0.27
beautifulsoup4>=4.12   # opcional (parser alterno); el scraper actual usa regex
curl_cffi>=0.7         # plusvalia: pasa Cloudflare imitando el fingerprint TLS de Chrome
