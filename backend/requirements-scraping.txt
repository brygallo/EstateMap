# Dependencias del scraping (proyecto de ingesta).
# NOTA: producción TAMBIÉN scrapea (el flujo de importación corre desde el
# admin, scrape -> MinIO en un paso), así que httpx y curl_cffi ya viven en
# requirements.txt y se instalan en la imagen Docker. Este archivo queda solo
# para el extra opcional del parser alterno.
#
#   pip install -r requirements-scraping.txt
#
beautifulsoup4>=4.12   # opcional (parser alterno); el scraper actual usa regex
