"""
GENERATED BY tools/specs — DO NOT EDIT BY HAND

Generated from specs/workflows/publish-property.yaml by tools/specs/gen_tests.py.
To change an assertion, change the case in the YAML and regenerate.
"""

import pytest

from real_estate.tests.spec_support import assert_outcome  # noqa: F401


pytestmark = [pytest.mark.django_db, pytest.mark.api]


# --- WFP-001: Sin sesión, el envío nunca llega a crear la propiedad ---

def test_wfp_001_llamada_directa_a_la_api_sin_token(spec_request):
    """
    SPEC:WFP-001 — Sin sesión, el envío nunca llega a crear la propiedad
    Case: Llamada directa a la API sin token
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='WFP-001',
        case_name='Llamada directa a la API sin token',
        expected_status=None,
    )


# --- WFP-012: El selector de estado ofrece exactamente los estados que el modelo admite ---

def test_wfp_012_publicar_con_un_estado_que_el_modelo_no_conoce(spec_request):
    """
    SPEC:WFP-012 — El selector de estado ofrece exactamente los estados que el modelo admite
    Case: Publicar con un estado que el modelo no conoce
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='authenticated',
        given=None,
        body={'status': 'sold', 'title': 'Propiedad vendida'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='WFP-012',
        case_name='Publicar con un estado que el modelo no conoce',
        expected_status=None,
    )
