"""
GENERATED BY tools/specs — DO NOT EDIT BY HAND

Generated from specs/domains/publication-resume.yaml by tools/specs/gen_tests.py.
To change an assertion, change the case in the YAML and regenerate.
"""

import pytest

from real_estate.tests.spec_support import assert_outcome  # noqa: F401


pytestmark = [pytest.mark.django_db, pytest.mark.api]


# --- RSM-001: Staff emite el enlace desde la bandeja de pendientes ---

def test_rsm_001_staff_emite_el_enlace_de_una_solicitud_nueva(spec_request):
    """
    SPEC:RSM-001 — Staff emite el enlace desde la bandeja de pendientes
    Case: staff emite el enlace de una solicitud nueva
    """
    response = spec_request(
        method='POST',
        path='/api/pending-publications/{pending_id}/resume-link/',
        role='staff',
        payload=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='RSM-001',
        case_name='staff emite el enlace de una solicitud nueva',
        expected_status=201,
    )

def test_rsm_001_un_usuario_autenticado_cualquiera_no_puede_emitirlo(spec_request):
    """
    SPEC:RSM-001 — Staff emite el enlace desde la bandeja de pendientes
    Case: un usuario autenticado cualquiera no puede emitirlo
    """
    response = spec_request(
        method='POST',
        path='/api/pending-publications/{pending_id}/resume-link/',
        role='not_owner',
        payload=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='RSM-001',
        case_name='un usuario autenticado cualquiera no puede emitirlo',
        expected_status=None,
    )

def test_rsm_001_un_anonimo_no_puede_emitirlo(spec_request):
    """
    SPEC:RSM-001 — Staff emite el enlace desde la bandeja de pendientes
    Case: un anónimo no puede emitirlo
    """
    response = spec_request(
        method='POST',
        path='/api/pending-publications/{pending_id}/resume-link/',
        role='anonymous',
        payload=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='RSM-001',
        case_name='un anónimo no puede emitirlo',
        expected_status=None,
    )


# --- RSM-002: El enlace caduca a los catorce días ---

def test_rsm_002_un_token_recien_emitido_resuelve_el_borrador(spec_request):
    """
    SPEC:RSM-002 — El enlace caduca a los catorce días
    Case: un token recién emitido resuelve el borrador
    """
    response = spec_request(
        method='GET',
        path='/api/publication-drafts/{token}/',
        role='anonymous',
        payload=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=410,
        rule_id='RSM-002',
        case_name='un token recién emitido resuelve el borrador',
        expected_status=200,
    )


# --- RSM-004: Staff puede revocar un enlace ya enviado ---

def test_rsm_004_staff_revoca_el_enlace_y_el_token_deja_de_resolver(spec_request):
    """
    SPEC:RSM-004 — Staff puede revocar un enlace ya enviado
    Case: staff revoca el enlace y el token deja de resolver
    """
    response = spec_request(
        method='POST',
        path='/api/pending-publications/{pending_id}/resume-link/revoke/',
        role='staff',
        payload=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='RSM-004',
        case_name='staff revoca el enlace y el token deja de resolver',
        expected_status=200,
    )

def test_rsm_004_un_usuario_autenticado_cualquiera_no_puede_revocarlo(spec_request):
    """
    SPEC:RSM-004 — Staff puede revocar un enlace ya enviado
    Case: un usuario autenticado cualquiera no puede revocarlo
    """
    response = spec_request(
        method='POST',
        path='/api/pending-publications/{pending_id}/resume-link/revoke/',
        role='not_owner',
        payload=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='RSM-004',
        case_name='un usuario autenticado cualquiera no puede revocarlo',
        expected_status=None,
    )


# --- RSM-005: El token solo abre el borrador, nunca la cuenta ---

def test_rsm_005_la_respuesta_trae_el_borrador_y_el_contacto_propio(spec_request):
    """
    SPEC:RSM-005 — El token solo abre el borrador, nunca la cuenta
    Case: la respuesta trae el borrador y el contacto propio
    """
    response = spec_request(
        method='GET',
        path='/api/publication-drafts/{token}/',
        role='anonymous',
        payload=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='RSM-005',
        case_name='la respuesta trae el borrador y el contacto propio',
        expected_status=200,
    )
