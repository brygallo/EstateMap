"""
GENERATED BY tools/specs — DO NOT EDIT BY HAND

Generated from specs/domains/leads.yaml by tools/specs/gen_tests.py.
To change an assertion, change the case in the YAML and regenerate.
"""

import pytest

from real_estate.tests.spec_support import assert_outcome  # noqa: F401


pytestmark = [pytest.mark.django_db, pytest.mark.api]


# --- LEAD-001: Crear un lead es público y no tiene límite de ritmo ---

def test_lead_001_visitante_anonimo_crea_un_lead(spec_request):
    """
    SPEC:LEAD-001 — Crear un lead es público y no tiene límite de ritmo
    Case: Visitante anónimo crea un lead
    """
    response = spec_request(
        method='POST',
        path='/api/leads/',
        role='anonymous',
        given=None,
        body={'property': '{property_id}', 'name': 'Interesado de spec', 'phone': '0991234567', 'email': 'interesado-spec@example.com', 'message': 'Me interesa esta propiedad.', 'source': 'property_page'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='LEAD-001',
        case_name='Visitante anónimo crea un lead',
        expected_status=201,
    )


# --- LEAD-005: Solo el dueño de la propiedad ve sus leads, staff los ve todos ---

def test_lead_005_visitante_anonimo_consulta_la_bandeja_de_leads(spec_request):
    """
    SPEC:LEAD-005 — Solo el dueño de la propiedad ve sus leads, staff los ve todos
    Case: Visitante anónimo consulta la bandeja de leads
    """
    response = spec_request(
        method='GET',
        path='/api/leads/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='LEAD-005',
        case_name='Visitante anónimo consulta la bandeja de leads',
        expected_status=None,
    )

def test_lead_005_dueno_de_la_propiedad_ve_solo_sus_propios_leads(spec_request):
    """
    SPEC:LEAD-005 — Solo el dueño de la propiedad ve sus leads, staff los ve todos
    Case: Dueño de la propiedad ve solo sus propios leads
    """
    response = spec_request(
        method='GET',
        path='/api/leads/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='LEAD-005',
        case_name='Dueño de la propiedad ve solo sus propios leads',
        expected_status=200,
    )

def test_lead_005_staff_ve_leads_de_cualquier_propiedad(spec_request):
    """
    SPEC:LEAD-005 — Solo el dueño de la propiedad ve sus leads, staff los ve todos
    Case: Staff ve leads de cualquier propiedad
    """
    response = spec_request(
        method='GET',
        path='/api/leads/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='LEAD-005',
        case_name='Staff ve leads de cualquier propiedad',
        expected_status=200,
    )


# --- LEAD-011: Solo staff lee la bandeja de solicitudes pendientes ---

def test_lead_011_visitante_anonimo_consulta_la_bandeja(spec_request):
    """
    SPEC:LEAD-011 — Solo staff lee la bandeja de solicitudes pendientes
    Case: Visitante anónimo consulta la bandeja
    """
    response = spec_request(
        method='GET',
        path='/api/pending-publications/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='LEAD-011',
        case_name='Visitante anónimo consulta la bandeja',
        expected_status=None,
    )

def test_lead_011_usuario_autenticado_sin_is_staff(spec_request):
    """
    SPEC:LEAD-011 — Solo staff lee la bandeja de solicitudes pendientes
    Case: Usuario autenticado sin is_staff
    """
    response = spec_request(
        method='GET',
        path='/api/pending-publications/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='LEAD-011',
        case_name='Usuario autenticado sin is_staff',
        expected_status=None,
    )

def test_lead_011_staff_consulta_la_bandeja_completa(spec_request):
    """
    SPEC:LEAD-011 — Solo staff lee la bandeja de solicitudes pendientes
    Case: Staff consulta la bandeja completa
    """
    response = spec_request(
        method='GET',
        path='/api/pending-publications/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='LEAD-011',
        case_name='Staff consulta la bandeja completa',
        expected_status=200,
    )
