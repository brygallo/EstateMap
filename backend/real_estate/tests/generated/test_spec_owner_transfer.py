"""
GENERATED BY tools/specs — DO NOT EDIT BY HAND

Generated from specs/domains/owner-transfer.yaml by tools/specs/gen_tests.py.
To change an assertion, change the case in the YAML and regenerate.
"""

import pytest

from real_estate.tests.spec_support import assert_outcome  # noqa: F401


pytestmark = [pytest.mark.django_db, pytest.mark.api]


# --- OWN-001: La transferencia es una acción propia, no un campo editable ---

def test_own_001_staff_transfiere_la_propiedad_a_otra_cuenta(spec_request):
    """
    SPEC:OWN-001 — La transferencia es una acción propia, no un campo editable
    Case: staff transfiere la propiedad a otra cuenta
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/transfer-owner/',
        role='staff',
        given=None,
        body={'email': 'destino@example.com'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='OWN-001',
        case_name='staff transfiere la propiedad a otra cuenta',
        expected_status=200,
    )

def test_own_001_un_usuario_autenticado_cualquiera_no_puede_transferir(spec_request):
    """
    SPEC:OWN-001 — La transferencia es una acción propia, no un campo editable
    Case: un usuario autenticado cualquiera no puede transferir
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/transfer-owner/',
        role='not_owner',
        given=None,
        body={'email': 'destino@example.com'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='OWN-001',
        case_name='un usuario autenticado cualquiera no puede transferir',
        expected_status=None,
    )

def test_own_001_el_propietario_actual_tampoco_puede_transferirla_el_mismo(spec_request):
    """
    SPEC:OWN-001 — La transferencia es una acción propia, no un campo editable
    Case: el propietario actual tampoco puede transferirla él mismo
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/transfer-owner/',
        role='owner',
        given=None,
        body={'email': 'destino@example.com'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='OWN-001',
        case_name='el propietario actual tampoco puede transferirla él mismo',
        expected_status=None,
    )

def test_own_001_un_anonimo_no_puede_transferir(spec_request):
    """
    SPEC:OWN-001 — La transferencia es una acción propia, no un campo editable
    Case: un anónimo no puede transferir
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/transfer-owner/',
        role='anonymous',
        given=None,
        body={'email': 'destino@example.com'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='OWN-001',
        case_name='un anónimo no puede transferir',
        expected_status=None,
    )


# --- OWN-002: El destino es una cuenta existente o una invitación por correo ---

def test_own_002_transferir_a_un_correo_sin_cuenta_la_crea_invitada(spec_request):
    """
    SPEC:OWN-002 — El destino es una cuenta existente o una invitación por correo
    Case: transferir a un correo sin cuenta la crea invitada
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/transfer-owner/',
        role='staff',
        given=None,
        body={'email': 'nuevo@example.com'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=400,
        rule_id='OWN-002',
        case_name='transferir a un correo sin cuenta la crea invitada',
        expected_status=200,
    )

def test_own_002_una_transferencia_sin_destino_se_rechaza(spec_request):
    """
    SPEC:OWN-002 — El destino es una cuenta existente o una invitación por correo
    Case: una transferencia sin destino se rechaza
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/transfer-owner/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='OWN-002',
        case_name='una transferencia sin destino se rechaza',
        expected_status=None,
    )
