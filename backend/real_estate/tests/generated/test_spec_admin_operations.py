"""
GENERATED BY tools/specs — DO NOT EDIT BY HAND

Generated from specs/domains/admin-operations.yaml by tools/specs/gen_tests.py.
To change an assertion, change the case in the YAML and regenerate.
"""

import pytest

from real_estate.tests.spec_support import assert_outcome  # noqa: F401


pytestmark = [pytest.mark.django_db, pytest.mark.api]


# --- ADM-001: Toda escritura administrativa deja una fila consultable ---

def test_adm_001_staff_lee_la_bitacora(spec_request):
    """
    SPEC:ADM-001 — Toda escritura administrativa deja una fila consultable
    Case: staff lee la bitácora
    """
    response = spec_request(
        method='GET',
        path='/api/admin/audit/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-001',
        case_name='staff lee la bitácora',
        expected_status=200,
    )

def test_adm_001_un_usuario_autenticado_cualquiera_no_puede_leerla(spec_request):
    """
    SPEC:ADM-001 — Toda escritura administrativa deja una fila consultable
    Case: un usuario autenticado cualquiera no puede leerla
    """
    response = spec_request(
        method='GET',
        path='/api/admin/audit/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='ADM-001',
        case_name='un usuario autenticado cualquiera no puede leerla',
        expected_status=None,
    )

def test_adm_001_un_anonimo_no_puede_leerla(spec_request):
    """
    SPEC:ADM-001 — Toda escritura administrativa deja una fila consultable
    Case: un anónimo no puede leerla
    """
    response = spec_request(
        method='GET',
        path='/api/admin/audit/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='ADM-001',
        case_name='un anónimo no puede leerla',
        expected_status=None,
    )


# --- ADM-003: El borrado administrativo es una papelera, no un DELETE ---

def test_adm_003_staff_envia_una_propiedad_a_la_papelera(spec_request):
    """
    SPEC:ADM-003 — El borrado administrativo es una papelera, no un DELETE
    Case: staff envía una propiedad a la papelera
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/properties/{property_id}/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-003',
        case_name='staff envía una propiedad a la papelera',
        expected_status=204,
    )

def test_adm_003_borrar_dos_veces_la_misma_propiedad_se_rechaza(spec_request):
    """
    SPEC:ADM-003 — El borrado administrativo es una papelera, no un DELETE
    Case: borrar dos veces la misma propiedad se rechaza
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/properties/{property_id}/',
        role='staff',
        given={'trashed': True},
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='ADM-003',
        case_name='borrar dos veces la misma propiedad se rechaza',
        expected_status=None,
    )


# --- ADM-004: Restaurar devuelve el anuncio al estado que ofrecía ---

def test_adm_004_staff_restaura_una_propiedad_de_la_papelera(spec_request):
    """
    SPEC:ADM-004 — Restaurar devuelve el anuncio al estado que ofrecía
    Case: staff restaura una propiedad de la papelera
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/restore/',
        role='staff',
        given={'trashed': True},
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-004',
        case_name='staff restaura una propiedad de la papelera',
        expected_status=200,
    )

def test_adm_004_restaurar_una_propiedad_que_no_esta_en_la_papelera_se_rechaza(spec_request):
    """
    SPEC:ADM-004 — Restaurar devuelve el anuncio al estado que ofrecía
    Case: restaurar una propiedad que no está en la papelera se rechaza
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/restore/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='ADM-004',
        case_name='restaurar una propiedad que no está en la papelera se rechaza',
        expected_status=None,
    )

def test_adm_004_un_anonimo_no_puede_restaurar(spec_request):
    """
    SPEC:ADM-004 — Restaurar devuelve el anuncio al estado que ofrecía
    Case: un anónimo no puede restaurar
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/restore/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='ADM-004',
        case_name='un anónimo no puede restaurar',
        expected_status=None,
    )


# --- ADM-005: El borrado definitivo solo se alcanza desde la papelera ---

def test_adm_005_staff_purga_una_propiedad_que_esta_en_la_papelera(spec_request):
    """
    SPEC:ADM-005 — El borrado definitivo solo se alcanza desde la papelera
    Case: staff purga una propiedad que está en la papelera
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/purge/',
        role='staff',
        given={'trashed': True},
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-005',
        case_name='staff purga una propiedad que está en la papelera',
        expected_status=204,
    )

def test_adm_005_purgar_una_propiedad_viva_se_rechaza(spec_request):
    """
    SPEC:ADM-005 — El borrado definitivo solo se alcanza desde la papelera
    Case: purgar una propiedad viva se rechaza
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/{property_id}/purge/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='ADM-005',
        case_name='purgar una propiedad viva se rechaza',
        expected_status=None,
    )


# --- ADM-008: El dashboard se sirve de caché versionada ---

def test_adm_008_la_segunda_lectura_seguida_llega_marcada_como_cacheada(spec_request):
    """
    SPEC:ADM-008 — El dashboard se sirve de caché versionada
    Case: la segunda lectura seguida llega marcada como cacheada
    """
    response = spec_request(
        method='GET',
        path='/api/admin/dashboard/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-008',
        case_name='la segunda lectura seguida llega marcada como cacheada',
        expected_status=200,
    )

def test_adm_008_refresh_1_recalcula_y_no_viene_marcada_como_cacheada(spec_request):
    """
    SPEC:ADM-008 — El dashboard se sirve de caché versionada
    Case: refresh=1 recalcula y no viene marcada como cacheada
    """
    response = spec_request(
        method='GET',
        path='/api/admin/dashboard/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-008',
        case_name='refresh=1 recalcula y no viene marcada como cacheada',
        expected_status=200,
    )


# --- ADM-010: El panel exporta a CSV sin cargar la tabla en memoria ---

def test_adm_010_staff_descarga_el_csv_de_usuarios(spec_request):
    """
    SPEC:ADM-010 — El panel exporta a CSV sin cargar la tabla en memoria
    Case: staff descarga el CSV de usuarios
    """
    response = spec_request(
        method='GET',
        path='/api/admin/export/users/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-010',
        case_name='staff descarga el CSV de usuarios',
        expected_status=200,
    )

def test_adm_010_un_usuario_autenticado_cualquiera_recibe_403(spec_request):
    """
    SPEC:ADM-010 — El panel exporta a CSV sin cargar la tabla en memoria
    Case: un usuario autenticado cualquiera recibe 403
    """
    response = spec_request(
        method='GET',
        path='/api/admin/export/users/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='ADM-010',
        case_name='un usuario autenticado cualquiera recibe 403',
        expected_status=None,
    )

def test_adm_010_un_anonimo_recibe_401(spec_request):
    """
    SPEC:ADM-010 — El panel exporta a CSV sin cargar la tabla en memoria
    Case: un anónimo recibe 401
    """
    response = spec_request(
        method='GET',
        path='/api/admin/export/users/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='ADM-010',
        case_name='un anónimo recibe 401',
        expected_status=None,
    )


# --- ADM-012: Una sola búsqueda cubre todo el panel ---

def test_adm_012_staff_busca_y_recibe_resultados_agrupados(spec_request):
    """
    SPEC:ADM-012 — Una sola búsqueda cubre todo el panel
    Case: staff busca y recibe resultados agrupados
    """
    response = spec_request(
        method='GET',
        path='/api/admin/search/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-012',
        case_name='staff busca y recibe resultados agrupados',
        expected_status=200,
    )

def test_adm_012_una_consulta_de_menos_de_dos_caracteres_devuelve_vacio_sin_consultar(spec_request):
    """
    SPEC:ADM-012 — Una sola búsqueda cubre todo el panel
    Case: una consulta de menos de dos caracteres devuelve vacío sin consultar
    """
    response = spec_request(
        method='GET',
        path='/api/admin/search/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-012',
        case_name='una consulta de menos de dos caracteres devuelve vacío sin consultar',
        expected_status=200,
    )

def test_adm_012_un_anonimo_no_puede_buscar(spec_request):
    """
    SPEC:ADM-012 — Una sola búsqueda cubre todo el panel
    Case: un anónimo no puede buscar
    """
    response = spec_request(
        method='GET',
        path='/api/admin/search/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='ADM-012',
        case_name='un anónimo no puede buscar',
        expected_status=None,
    )


# --- ADM-013: El diagnóstico dice por qué no se ve una propiedad y qué hacer ---

def test_adm_013_una_propiedad_sin_coordenadas_ni_poligono_declara_el_bloqueo_no_coordinates(spec_request):
    """
    SPEC:ADM-013 — El diagnóstico dice por qué no se ve una propiedad y qué hacer
    Case: una propiedad sin coordenadas ni polígono declara el bloqueo no_coordinates
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/{property_id}/diagnostics/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-013',
        case_name='una propiedad sin coordenadas ni polígono declara el bloqueo no_coordinates',
        expected_status=200,
    )

def test_adm_013_un_anonimo_no_puede_pedir_el_diagnostico(spec_request):
    """
    SPEC:ADM-013 — El diagnóstico dice por qué no se ve una propiedad y qué hacer
    Case: un anónimo no puede pedir el diagnóstico
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/{property_id}/diagnostics/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='ADM-013',
        case_name='un anónimo no puede pedir el diagnóstico',
        expected_status=None,
    )


# --- ADM-014: La cobertura de páginas usa los mismos umbrales que el sitio ---

def test_adm_014_una_ciudad_con_cinco_anuncios_del_mismo_tipo_aparece_como_pagina_abierta(spec_request):
    """
    SPEC:ADM-014 — La cobertura de páginas usa los mismos umbrales que el sitio
    Case: una ciudad con cinco anuncios del mismo tipo aparece como página abierta
    """
    response = spec_request(
        method='GET',
        path='/api/admin/seo-health/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='ADM-014',
        case_name='una ciudad con cinco anuncios del mismo tipo aparece como página abierta',
        expected_status=200,
    )
