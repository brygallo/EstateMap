"""
GENERATED BY tools/specs — DO NOT EDIT BY HAND

Generated from specs/domains/properties.yaml by tools/specs/gen_tests.py.
To change an assertion, change the case in the YAML and regenerate.
"""

import pytest

from real_estate.tests.spec_support import assert_outcome  # noqa: F401


pytestmark = [pytest.mark.django_db, pytest.mark.api]


# --- PROP-003: Solo el dueño autenticado ve sus propiedades inactivas ---

def test_prop_003_un_visitante_anonimo_no_puede_consultar_la_bandeja(spec_request):
    """
    SPEC:PROP-003 — Solo el dueño autenticado ve sus propiedades inactivas
    Case: Un visitante anónimo no puede consultar la bandeja
    """
    response = spec_request(
        method='GET',
        path='/api/properties/my_properties/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PROP-003',
        case_name='Un visitante anónimo no puede consultar la bandeja',
        expected_status=None,
    )

def test_prop_003_un_usuario_autenticado_consulta_sus_propias_propiedades(spec_request):
    """
    SPEC:PROP-003 — Solo el dueño autenticado ve sus propiedades inactivas
    Case: Un usuario autenticado consulta sus propias propiedades
    """
    response = spec_request(
        method='GET',
        path='/api/properties/my_properties/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PROP-003',
        case_name='Un usuario autenticado consulta sus propias propiedades',
        expected_status=200,
    )


# --- PROP-015: Solo el dueño puede modificar o borrar su propiedad ---

def test_prop_015_el_dueno_borra_su_propiedad(spec_request):
    """
    SPEC:PROP-015 — Solo el dueño puede modificar o borrar su propiedad
    Case: El dueño borra su propiedad
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PROP-015',
        case_name='El dueño borra su propiedad',
        expected_status=204,
    )

def test_prop_015_otro_usuario_autenticado_no_puede_borrarla(spec_request):
    """
    SPEC:PROP-015 — Solo el dueño puede modificar o borrar su propiedad
    Case: Otro usuario autenticado no puede borrarla
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PROP-015',
        case_name='Otro usuario autenticado no puede borrarla',
        expected_status=None,
    )

def test_prop_015_un_visitante_anonimo_no_puede_borrarla(spec_request):
    """
    SPEC:PROP-015 — Solo el dueño puede modificar o borrar su propiedad
    Case: Un visitante anónimo no puede borrarla
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PROP-015',
        case_name='Un visitante anónimo no puede borrarla',
        expected_status=None,
    )


# --- PROP-034: Un anuncio cerrado conserva su ficha y su código corto ---

def test_prop_034_la_ficha_de_un_anuncio_vendido_responde_a_un_anonimo(spec_request):
    """
    SPEC:PROP-034 — Un anuncio cerrado conserva su ficha y su código corto
    Case: La ficha de un anuncio vendido responde a un anónimo
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/',
        role='anonymous',
        given={'closed_reason': 'sold'},
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PROP-034',
        case_name='La ficha de un anuncio vendido responde a un anónimo',
        expected_status=200,
    )

def test_prop_034_el_codigo_corto_de_un_anuncio_vendido_resuelve(spec_request):
    """
    SPEC:PROP-034 — Un anuncio cerrado conserva su ficha y su código corto
    Case: El código corto de un anuncio vendido resuelve
    """
    response = spec_request(
        method='GET',
        path='/api/properties/code/{code}/',
        role='anonymous',
        given={'closed_reason': 'sold'},
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PROP-034',
        case_name='El código corto de un anuncio vendido resuelve',
        expected_status=200,
    )

def test_prop_034_el_codigo_corto_de_un_anuncio_retirado_tambien_resuelve(spec_request):
    """
    SPEC:PROP-034 — Un anuncio cerrado conserva su ficha y su código corto
    Case: El código corto de un anuncio retirado también resuelve
    """
    response = spec_request(
        method='GET',
        path='/api/properties/code/{code}/',
        role='anonymous',
        given={'closed_reason': 'withdrawn'},
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PROP-034',
        case_name='El código corto de un anuncio retirado también resuelve',
        expected_status=200,
    )

def test_prop_034_un_anuncio_solo_inactivo_no_resuelve(spec_request):
    """
    SPEC:PROP-034 — Un anuncio cerrado conserva su ficha y su código corto
    Case: Un anuncio solo inactivo no resuelve
    """
    response = spec_request(
        method='GET',
        path='/api/properties/code/{code}/',
        role='anonymous',
        given={'status': 'inactive'},
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=404,
        rule_id='PROP-034',
        case_name='Un anuncio solo inactivo no resuelve',
        expected_status=None,
    )

def test_prop_034_el_dueno_alcanza_su_anuncio_inactivo(spec_request):
    """
    SPEC:PROP-034 — Un anuncio cerrado conserva su ficha y su código corto
    Case: El dueño alcanza su anuncio inactivo
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/',
        role='owner',
        given={'status': 'inactive'},
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PROP-034',
        case_name='El dueño alcanza su anuncio inactivo',
        expected_status=200,
    )

def test_prop_034_el_dueno_tampoco_alcanza_su_anuncio_inactivo_por_el_codigo_corto(spec_request):
    """
    SPEC:PROP-034 — Un anuncio cerrado conserva su ficha y su código corto
    Case: El dueño tampoco alcanza su anuncio inactivo por el código corto
    """
    response = spec_request(
        method='GET',
        path='/api/properties/code/{code}/',
        role='owner',
        given={'status': 'inactive'},
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=404,
        rule_id='PROP-034',
        case_name='El dueño tampoco alcanza su anuncio inactivo por el código corto',
        expected_status=None,
    )


# --- PROP-036: Un precio o una superficie negativos no se guardan ---

def test_prop_036_publicar_con_precio_negativo(spec_request):
    """
    SPEC:PROP-036 — Un precio o una superficie negativos no se guardan
    Case: Publicar con precio negativo
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='authenticated',
        given=None,
        body={'title': 'Precio imposible', 'price': '-150000'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='PROP-036',
        case_name='Publicar con precio negativo',
        expected_status=None,
    )

def test_prop_036_publicar_con_area_negativa(spec_request):
    """
    SPEC:PROP-036 — Un precio o una superficie negativos no se guardan
    Case: Publicar con área negativa
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='authenticated',
        given=None,
        body={'title': 'Área imposible', 'price': '1000', 'area': -999},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='PROP-036',
        case_name='Publicar con área negativa',
        expected_status=None,
    )


# --- PROP-037: La descripción tiene un tope de caracteres ---

def test_prop_037_publicar_con_una_descripcion_por_encima_del_tope(spec_request):
    """
    SPEC:PROP-037 — La descripción tiene un tope de caracteres
    Case: Publicar con una descripción por encima del tope
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='authenticated',
        given=None,
        body={'title': 'Descripción interminable', 'price': '1000', 'description': '{description_over_limit}'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=400,
        rule_id='PROP-037',
        case_name='Publicar con una descripción por encima del tope',
        expected_status=None,
    )
