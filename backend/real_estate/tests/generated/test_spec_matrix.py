"""
GENERATED BY tools/specs — DO NOT EDIT BY HAND

Generated from specs/permissions/matrix.yaml by tools/specs/gen_tests.py.
To change an assertion, change the case in the YAML and regenerate.
"""

import pytest

from real_estate.tests.spec_support import assert_outcome  # noqa: F401


pytestmark = [pytest.mark.django_db, pytest.mark.api]


# --- PERM-001: El catálogo de propiedades se lee sin cuenta ---

def test_perm_001_un_visitante_anonimo_lista_el_catalogo(spec_request):
    """
    SPEC:PERM-001 — El catálogo de propiedades se lee sin cuenta
    Case: un visitante anónimo lista el catálogo
    """
    response = spec_request(
        method='GET',
        path='/api/properties/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-001',
        case_name='un visitante anónimo lista el catálogo',
        expected_status=200,
    )

def test_perm_001_un_usuario_autenticado_lista_el_catalogo(spec_request):
    """
    SPEC:PERM-001 — El catálogo de propiedades se lee sin cuenta
    Case: un usuario autenticado lista el catálogo
    """
    response = spec_request(
        method='GET',
        path='/api/properties/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-001',
        case_name='un usuario autenticado lista el catálogo',
        expected_status=200,
    )

def test_perm_001_el_renderizado_en_servidor_de_next_js_lista_el_catalogo(spec_request):
    """
    SPEC:PERM-001 — El catálogo de propiedades se lee sin cuenta
    Case: el renderizado en servidor de Next.js lista el catálogo
    """
    response = spec_request(
        method='GET',
        path='/api/properties/',
        role='internal',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-001',
        case_name='el renderizado en servidor de Next.js lista el catálogo',
        expected_status=200,
    )


# --- PERM-002: El detalle de una propiedad se lee sin cuenta ---

def test_perm_002_un_visitante_anonimo_abre_la_ficha(spec_request):
    """
    SPEC:PERM-002 — El detalle de una propiedad se lee sin cuenta
    Case: un visitante anónimo abre la ficha
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-002',
        case_name='un visitante anónimo abre la ficha',
        expected_status=200,
    )

def test_perm_002_un_tercero_autenticado_abre_la_ficha(spec_request):
    """
    SPEC:PERM-002 — El detalle de una propiedad se lee sin cuenta
    Case: un tercero autenticado abre la ficha
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-002',
        case_name='un tercero autenticado abre la ficha',
        expected_status=200,
    )

def test_perm_002_el_propietario_abre_su_propia_ficha(spec_request):
    """
    SPEC:PERM-002 — El detalle de una propiedad se lee sin cuenta
    Case: el propietario abre su propia ficha
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-002',
        case_name='el propietario abre su propia ficha',
        expected_status=200,
    )


# --- PERM-003: Publicar una propiedad exige haber iniciado sesión ---

def test_perm_003_un_anonimo_no_puede_publicar(spec_request):
    """
    SPEC:PERM-003 — Publicar una propiedad exige haber iniciado sesión
    Case: un anónimo no puede publicar
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-003',
        case_name='un anónimo no puede publicar',
        expected_status=None,
    )

def test_perm_003_un_usuario_autenticado_publica_una_propiedad(spec_request):
    """
    SPEC:PERM-003 — Publicar una propiedad exige haber iniciado sesión
    Case: un usuario autenticado publica una propiedad
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='authenticated',
        given=None,
        body={'title': 'Propiedad creada por spec', 'description': 'Anuncio mínimo válido para el caso permitido.', 'property_type': 'land', 'status': 'for_sale', 'city': 'Macas', 'province': 'Morona Santiago', 'latitude': -2.308, 'longitude': -78.118, 'area': 500, 'price': 25000},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-003',
        case_name='un usuario autenticado publica una propiedad',
        expected_status=201,
    )


# --- PERM-004: La propiedad la edita su dueño o un administrador ---

def test_perm_004_el_propietario_edita_su_propiedad(spec_request):
    """
    SPEC:PERM-004 — La propiedad la edita su dueño o un administrador
    Case: el propietario edita su propiedad
    """
    response = spec_request(
        method='PATCH',
        path='/api/properties/{property_id}/',
        role='owner',
        given=None,
        body={'title': 'Propiedad de spec editada'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-004',
        case_name='el propietario edita su propiedad',
        expected_status=200,
    )

def test_perm_004_un_tercero_autenticado_no_puede_editarla(spec_request):
    """
    SPEC:PERM-004 — La propiedad la edita su dueño o un administrador
    Case: un tercero autenticado no puede editarla
    """
    response = spec_request(
        method='PATCH',
        path='/api/properties/{property_id}/',
        role='not_owner',
        given=None,
        body={'title': 'Intento de secuestro'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-004',
        case_name='un tercero autenticado no puede editarla',
        expected_status=None,
    )

def test_perm_004_un_usuario_staff_si_puede_editarla_por_la_ruta_publica(spec_request):
    """
    SPEC:PERM-004 — La propiedad la edita su dueño o un administrador
    Case: un usuario staff sí puede editarla por la ruta pública
    """
    response = spec_request(
        method='PATCH',
        path='/api/properties/{property_id}/',
        role='staff',
        given=None,
        body={'title': 'Edición desde staff'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-004',
        case_name='un usuario staff sí puede editarla por la ruta pública',
        expected_status=200,
    )

def test_perm_004_un_anonimo_no_puede_editarla(spec_request):
    """
    SPEC:PERM-004 — La propiedad la edita su dueño o un administrador
    Case: un anónimo no puede editarla
    """
    response = spec_request(
        method='PATCH',
        path='/api/properties/{property_id}/',
        role='anonymous',
        given=None,
        body={'title': 'Intento anónimo'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-004',
        case_name='un anónimo no puede editarla',
        expected_status=None,
    )


# --- PERM-005: La propiedad la elimina su dueño o un administrador ---

def test_perm_005_el_propietario_elimina_su_propiedad(spec_request):
    """
    SPEC:PERM-005 — La propiedad la elimina su dueño o un administrador
    Case: el propietario elimina su propiedad
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-005',
        case_name='el propietario elimina su propiedad',
        expected_status=204,
    )

def test_perm_005_un_tercero_autenticado_no_puede_eliminarla(spec_request):
    """
    SPEC:PERM-005 — La propiedad la elimina su dueño o un administrador
    Case: un tercero autenticado no puede eliminarla
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-005',
        case_name='un tercero autenticado no puede eliminarla',
        expected_status=None,
    )

def test_perm_005_un_usuario_staff_elimina_una_propiedad_ajena(spec_request):
    """
    SPEC:PERM-005 — La propiedad la elimina su dueño o un administrador
    Case: un usuario staff elimina una propiedad ajena
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-005',
        case_name='un usuario staff elimina una propiedad ajena',
        expected_status=204,
    )

def test_perm_005_un_anonimo_no_puede_eliminarla(spec_request):
    """
    SPEC:PERM-005 — La propiedad la elimina su dueño o un administrador
    Case: un anónimo no puede eliminarla
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-005',
        case_name='un anónimo no puede eliminarla',
        expected_status=None,
    )


# --- PERM-006: El inventario exige sesión y su alcance depende del rol ---

def test_perm_006_un_anonimo_no_puede_consultar_inventario_propio(spec_request):
    """
    SPEC:PERM-006 — El inventario exige sesión y su alcance depende del rol
    Case: un anónimo no puede consultar inventario propio
    """
    response = spec_request(
        method='GET',
        path='/api/properties/my_properties/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-006',
        case_name='un anónimo no puede consultar inventario propio',
        expected_status=None,
    )

def test_perm_006_el_propietario_ve_su_inventario(spec_request):
    """
    SPEC:PERM-006 — El inventario exige sesión y su alcance depende del rol
    Case: el propietario ve su inventario
    """
    response = spec_request(
        method='GET',
        path='/api/properties/my_properties/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-006',
        case_name='el propietario ve su inventario',
        expected_status=200,
    )

def test_perm_006_otro_usuario_recibe_su_propia_lista_no_la_ajena(spec_request):
    """
    SPEC:PERM-006 — El inventario exige sesión y su alcance depende del rol
    Case: otro usuario recibe su propia lista, no la ajena
    """
    response = spec_request(
        method='GET',
        path='/api/properties/my_properties/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-006',
        case_name='otro usuario recibe su propia lista, no la ajena',
        expected_status=200,
    )

def test_perm_006_un_administrador_recibe_el_catalogo_entero(spec_request):
    """
    SPEC:PERM-006 — El inventario exige sesión y su alcance depende del rol
    Case: un administrador recibe el catálogo entero
    """
    response = spec_request(
        method='GET',
        path='/api/properties/my_properties/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-006',
        case_name='un administrador recibe el catálogo entero',
        expected_status=200,
    )


# --- PERM-007: Una cuenta con el correo sin verificar puede publicar ---

def test_perm_007_una_cuenta_sin_verificar_publica_una_propiedad(spec_request):
    """
    SPEC:PERM-007 — Una cuenta con el correo sin verificar puede publicar
    Case: una cuenta sin verificar publica una propiedad
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='unverified',
        given=None,
        body={'title': 'Propiedad de cuenta sin verificar', 'description': 'Anuncio mínimo válido para el caso permitido.', 'property_type': 'land', 'status': 'for_sale', 'city': 'Macas', 'province': 'Morona Santiago', 'latitude': -2.308, 'longitude': -78.118, 'area': 500, 'price': 25000},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-007',
        case_name='una cuenta sin verificar publica una propiedad',
        expected_status=201,
    )


# --- PERM-008: Publicar debería exigir el correo verificado ---

@pytest.mark.skip(reason="PERM-008 is 'proposed': no code implements it yet")
def test_perm_008_una_cuenta_sin_verificar_no_deberia_poder_publicar(spec_request):
    """
    SPEC:PERM-008 — Publicar debería exigir el correo verificado
    Case: una cuenta sin verificar no debería poder publicar
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='unverified',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-008',
        case_name='una cuenta sin verificar no debería poder publicar',
        expected_status=None,
    )

@pytest.mark.skip(reason="PERM-008 is 'proposed': no code implements it yet")
def test_perm_008_una_cuenta_verificada_si_deberia_poder_publicar(spec_request):
    """
    SPEC:PERM-008 — Publicar debería exigir el correo verificado
    Case: una cuenta verificada sí debería poder publicar
    """
    response = spec_request(
        method='POST',
        path='/api/properties/',
        role='authenticated',
        given=None,
        body={'title': 'Propiedad de cuenta verificada', 'description': 'Anuncio mínimo válido para el caso permitido.', 'property_type': 'land', 'status': 'for_sale', 'city': 'Macas', 'province': 'Morona Santiago', 'latitude': -2.308, 'longitude': -78.118, 'area': 500, 'price': 25000},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-008',
        case_name='una cuenta verificada sí debería poder publicar',
        expected_status=201,
    )


# --- PERM-010: delete_image exige ser propietario ---

def test_perm_010_un_anonimo_queda_fuera(spec_request):
    """
    SPEC:PERM-010 — delete_image exige ser propietario
    Case: un anónimo queda fuera
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/delete_image/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-010',
        case_name='un anónimo queda fuera',
        expected_status=None,
    )

def test_perm_010_un_tercero_autenticado_no_puede_borrar_imagenes_ajenas(spec_request):
    """
    SPEC:PERM-010 — delete_image exige ser propietario
    Case: un tercero autenticado no puede borrar imágenes ajenas
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/delete_image/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-010',
        case_name='un tercero autenticado no puede borrar imágenes ajenas',
        expected_status=None,
    )

def test_perm_010_el_propietario_supera_la_barrera(spec_request):
    """
    SPEC:PERM-010 — delete_image exige ser propietario
    Case: el propietario supera la barrera
    """
    response = spec_request(
        method='DELETE',
        path='/api/properties/{property_id}/delete_image/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-010',
        case_name='el propietario supera la barrera',
        expected_status=None,
    )


# --- PERM-011: El contexto comercial no sensible permanece público ---

def test_perm_011_un_visitante_anonimo_obtiene_el_contexto_publico(spec_request):
    """
    SPEC:PERM-011 — El contexto comercial no sensible permanece público
    Case: un visitante anónimo obtiene el contexto público
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/intelligence/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-011',
        case_name='un visitante anónimo obtiene el contexto público',
        expected_status=200,
    )

def test_perm_011_un_tercero_autenticado_tambien(spec_request):
    """
    SPEC:PERM-011 — El contexto comercial no sensible permanece público
    Case: un tercero autenticado también
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/intelligence/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-011',
        case_name='un tercero autenticado también',
        expected_status=200,
    )


# --- PERM-012: Las métricas de demanda son privadas ---

def test_perm_012_un_anonimo_no_deberia_ver_el_contador_de_visitas(spec_request):
    """
    SPEC:PERM-012 — Las métricas de demanda son privadas
    Case: un anónimo no debería ver el contador de visitas
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/intelligence/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-012',
        case_name='un anónimo no debería ver el contador de visitas',
        expected_status=200,
    )

def test_perm_012_el_propietario_si_deberia_verlo(spec_request):
    """
    SPEC:PERM-012 — Las métricas de demanda son privadas
    Case: el propietario sí debería verlo
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/intelligence/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-012',
        case_name='el propietario sí debería verlo',
        expected_status=None,
    )


# --- PERM-013: El payload del mapa es público y está limitado contra el scraping ---

def test_perm_013_un_visitante_anonimo_carga_el_mapa(spec_request):
    """
    SPEC:PERM-013 — El payload del mapa es público y está limitado contra el scraping
    Case: un visitante anónimo carga el mapa
    """
    response = spec_request(
        method='GET',
        path='/api/properties/map_points/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-013',
        case_name='un visitante anónimo carga el mapa',
        expected_status=200,
    )

def test_perm_013_el_renderizado_en_servidor_carga_el_mapa_sin_consumir_cuota(spec_request):
    """
    SPEC:PERM-013 — El payload del mapa es público y está limitado contra el scraping
    Case: el renderizado en servidor carga el mapa sin consumir cuota
    """
    response = spec_request(
        method='GET',
        path='/api/properties/map_points/',
        role='internal',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-013',
        case_name='el renderizado en servidor carga el mapa sin consumir cuota',
        expected_status=200,
    )

def test_perm_013_un_usuario_staff_carga_el_mapa(spec_request):
    """
    SPEC:PERM-013 — El payload del mapa es público y está limitado contra el scraping
    Case: un usuario staff carga el mapa
    """
    response = spec_request(
        method='GET',
        path='/api/properties/map_points/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-013',
        case_name='un usuario staff carga el mapa',
        expected_status=200,
    )


# --- PERM-014: Los contadores agregados del catálogo son públicos ---

def test_perm_014_un_visitante_anonimo_obtiene_los_totales(spec_request):
    """
    SPEC:PERM-014 — Los contadores agregados del catálogo son públicos
    Case: un visitante anónimo obtiene los totales
    """
    response = spec_request(
        method='GET',
        path='/api/properties/summary/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-014',
        case_name='un visitante anónimo obtiene los totales',
        expected_status=200,
    )


# --- PERM-015: Las ubicaciones con inventario son públicas ---

def test_perm_015_un_visitante_anonimo_obtiene_las_ubicaciones(spec_request):
    """
    SPEC:PERM-015 — Las ubicaciones con inventario son públicas
    Case: un visitante anónimo obtiene las ubicaciones
    """
    response = spec_request(
        method='GET',
        path='/api/properties/locations/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-015',
        case_name='un visitante anónimo obtiene las ubicaciones',
        expected_status=200,
    )


# --- PERM-016: El catálogo estable de cantones es público ---

def test_perm_016_un_visitante_anonimo_obtiene_el_catalogo_de_cantones(spec_request):
    """
    SPEC:PERM-016 — El catálogo estable de cantones es público
    Case: un visitante anónimo obtiene el catálogo de cantones
    """
    response = spec_request(
        method='GET',
        path='/api/properties/catalog/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-016',
        case_name='un visitante anónimo obtiene el catálogo de cantones',
        expected_status=200,
    )


# --- PERM-017: La lista de propietarios con inventario es pública ---

def test_perm_017_un_visitante_anonimo_obtiene_la_lista_de_propietarios(spec_request):
    """
    SPEC:PERM-017 — La lista de propietarios con inventario es pública
    Case: un visitante anónimo obtiene la lista de propietarios
    """
    response = spec_request(
        method='GET',
        path='/api/properties/owners/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-017',
        case_name='un visitante anónimo obtiene la lista de propietarios',
        expected_status=200,
    )


# --- PERM-018: Los indicadores de mercado son públicos ---

def test_perm_018_un_visitante_anonimo_obtiene_los_indicadores(spec_request):
    """
    SPEC:PERM-018 — Los indicadores de mercado son públicos
    Case: un visitante anónimo obtiene los indicadores
    """
    response = spec_request(
        method='GET',
        path='/api/market-stats/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-018',
        case_name='un visitante anónimo obtiene los indicadores',
        expected_status=200,
    )

def test_perm_018_el_renderizado_en_servidor_obtiene_los_indicadores(spec_request):
    """
    SPEC:PERM-018 — Los indicadores de mercado son públicos
    Case: el renderizado en servidor obtiene los indicadores
    """
    response = spec_request(
        method='GET',
        path='/api/market-stats/',
        role='internal',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-018',
        case_name='el renderizado en servidor obtiene los indicadores',
        expected_status=200,
    )


# --- PERM-019: Las tablas de provincias y cantones se leen sin cuenta ---

def test_perm_019_un_visitante_anonimo_consulta_las_provincias(spec_request):
    """
    SPEC:PERM-019 — Las tablas de provincias y cantones se leen sin cuenta
    Case: un visitante anónimo consulta las provincias
    """
    response = spec_request(
        method='GET',
        path='/api/provinces/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-019',
        case_name='un visitante anónimo consulta las provincias',
        expected_status=200,
    )


# --- PERM-020: La geografía no se puede escribir desde la API ---

def test_perm_020_un_anonimo_no_puede_crear_provincias(spec_request):
    """
    SPEC:PERM-020 — La geografía no se puede escribir desde la API
    Case: un anónimo no puede crear provincias
    """
    response = spec_request(
        method='POST',
        path='/api/provinces/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=405,
        rule_id='PERM-020',
        case_name='un anónimo no puede crear provincias',
        expected_status=None,
    )

def test_perm_020_ni_siquiera_staff_puede_crear_provincias_por_la_api(spec_request):
    """
    SPEC:PERM-020 — La geografía no se puede escribir desde la API
    Case: ni siquiera staff puede crear provincias por la API
    """
    response = spec_request(
        method='POST',
        path='/api/provinces/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=405,
        rule_id='PERM-020',
        case_name='ni siquiera staff puede crear provincias por la API',
        expected_status=None,
    )


# --- PERM-021: El proxy de imágenes es público y sin restricción de objeto ---

def test_perm_021_un_anonimo_alcanza_el_proxy_sin_credenciales(spec_request):
    """
    SPEC:PERM-021 — El proxy de imágenes es público y sin restricción de objeto
    Case: un anónimo alcanza el proxy sin credenciales
    """
    response = spec_request(
        method='GET',
        path='/api/media/spec-inexistente.webp',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-021',
        case_name='un anónimo alcanza el proxy sin credenciales',
        expected_status=404,
    )


# --- PERM-023: Las imágenes en staging son públicas mientras se optimizan ---

def test_perm_023_un_anonimo_alcanza_la_vista_sin_credenciales(spec_request):
    """
    SPEC:PERM-023 — Las imágenes en staging son públicas mientras se optimizan
    Case: un anónimo alcanza la vista sin credenciales
    """
    response = spec_request(
        method='GET',
        path='/api/pending-image/{id}/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-023',
        case_name='un anónimo alcanza la vista sin credenciales',
        expected_status=404,
    )


# --- PERM-024: El formulario de contacto es público ---

def test_perm_024_un_anonimo_envia_el_formulario_de_contacto(spec_request):
    """
    SPEC:PERM-024 — El formulario de contacto es público
    Case: un anónimo envía el formulario de contacto
    """
    response = spec_request(
        method='POST',
        path='/api/leads/',
        role='anonymous',
        given=None,
        body={'property': '{property_id}', 'name': 'Interesado de spec', 'phone': '0991234567', 'email': 'interesado-spec@example.com', 'message': 'Me interesa esta propiedad.', 'source': 'property_page', 'status': 'contacted'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-024',
        case_name='un anónimo envía el formulario de contacto',
        expected_status=201,
    )

def test_perm_024_un_usuario_autenticado_tambien_puede_enviarlo(spec_request):
    """
    SPEC:PERM-024 — El formulario de contacto es público
    Case: un usuario autenticado también puede enviarlo
    """
    response = spec_request(
        method='POST',
        path='/api/leads/',
        role='authenticated',
        given=None,
        body={'property': '{property_id}', 'name': 'Interesado con cuenta', 'phone': '0991234567', 'email': 'interesado-auth@example.com', 'message': 'Me interesa esta propiedad.', 'source': 'property_page'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-024',
        case_name='un usuario autenticado también puede enviarlo',
        expected_status=201,
    )


# --- PERM-026: La bandeja de leads exige sesión ---

def test_perm_026_un_anonimo_no_puede_listar_leads(spec_request):
    """
    SPEC:PERM-026 — La bandeja de leads exige sesión
    Case: un anónimo no puede listar leads
    """
    response = spec_request(
        method='GET',
        path='/api/leads/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-026',
        case_name='un anónimo no puede listar leads',
        expected_status=None,
    )

def test_perm_026_el_propietario_lista_los_leads_de_sus_propiedades(spec_request):
    """
    SPEC:PERM-026 — La bandeja de leads exige sesión
    Case: el propietario lista los leads de sus propiedades
    """
    response = spec_request(
        method='GET',
        path='/api/leads/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-026',
        case_name='el propietario lista los leads de sus propiedades',
        expected_status=200,
    )

def test_perm_026_otro_usuario_recibe_una_lista_vacia_no_la_ajena(spec_request):
    """
    SPEC:PERM-026 — La bandeja de leads exige sesión
    Case: otro usuario recibe una lista vacía, no la ajena
    """
    response = spec_request(
        method='GET',
        path='/api/leads/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-026',
        case_name='otro usuario recibe una lista vacía, no la ajena',
        expected_status=200,
    )

def test_perm_026_staff_lista_todos_los_leads(spec_request):
    """
    SPEC:PERM-026 — La bandeja de leads exige sesión
    Case: staff lista todos los leads
    """
    response = spec_request(
        method='GET',
        path='/api/leads/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-026',
        case_name='staff lista todos los leads',
        expected_status=200,
    )


# --- PERM-028: La solicitud de publicación asistida es pública y está limitada ---

def test_perm_028_un_anonimo_registra_una_solicitud(spec_request):
    """
    SPEC:PERM-028 — La solicitud de publicación asistida es pública y está limitada
    Case: un anónimo registra una solicitud
    """
    response = spec_request(
        method='POST',
        path='/api/pending-publications/',
        role='anonymous',
        given=None,
        body={'title': 'Solicitud de spec', 'contact_phone': '0991234567', 'contact_email': 'solicitud-spec@example.com', 'city': 'Macas', 'province': 'Morona Santiago', 'property_type': 'land', 'operation': 'for_sale', 'price': '32000', 'source': 'account_required', 'status': 'converted'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-028',
        case_name='un anónimo registra una solicitud',
        expected_status=201,
    )


# --- PERM-029: La bandeja de publicación asistida es solo para staff ---

def test_perm_029_un_anonimo_no_accede_a_la_bandeja(spec_request):
    """
    SPEC:PERM-029 — La bandeja de publicación asistida es solo para staff
    Case: un anónimo no accede a la bandeja
    """
    response = spec_request(
        method='GET',
        path='/api/pending-publications/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-029',
        case_name='un anónimo no accede a la bandeja',
        expected_status=None,
    )

def test_perm_029_un_usuario_autenticado_normal_no_accede_a_la_bandeja(spec_request):
    """
    SPEC:PERM-029 — La bandeja de publicación asistida es solo para staff
    Case: un usuario autenticado normal no accede a la bandeja
    """
    response = spec_request(
        method='GET',
        path='/api/pending-publications/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-029',
        case_name='un usuario autenticado normal no accede a la bandeja',
        expected_status=None,
    )

def test_perm_029_el_propietario_de_propiedades_tampoco_accede(spec_request):
    """
    SPEC:PERM-029 — La bandeja de publicación asistida es solo para staff
    Case: el propietario de propiedades tampoco accede
    """
    response = spec_request(
        method='GET',
        path='/api/pending-publications/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-029',
        case_name='el propietario de propiedades tampoco accede',
        expected_status=None,
    )

def test_perm_029_staff_accede_a_la_bandeja(spec_request):
    """
    SPEC:PERM-029 — La bandeja de publicación asistida es solo para staff
    Case: staff accede a la bandeja
    """
    response = spec_request(
        method='GET',
        path='/api/pending-publications/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-029',
        case_name='staff accede a la bandeja',
        expected_status=200,
    )


# --- PERM-030: La captura de eventos de analítica es pública y está limitada ---

def test_perm_030_un_anonimo_emite_un_evento(spec_request):
    """
    SPEC:PERM-030 — La captura de eventos de analítica es pública y está limitada
    Case: un anónimo emite un evento
    """
    response = spec_request(
        method='POST',
        path='/api/activity-events/',
        role='anonymous',
        given=None,
        body={'event_name': 'spec_probe', 'path': '/spec'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-030',
        case_name='un anónimo emite un evento',
        expected_status=201,
    )

def test_perm_030_un_usuario_autenticado_emite_un_evento(spec_request):
    """
    SPEC:PERM-030 — La captura de eventos de analítica es pública y está limitada
    Case: un usuario autenticado emite un evento
    """
    response = spec_request(
        method='POST',
        path='/api/activity-events/',
        role='authenticated',
        given=None,
        body={'event_name': 'spec_probe', 'path': '/spec'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-030',
        case_name='un usuario autenticado emite un evento',
        expected_status=201,
    )


# --- PERM-031: El registro de actividad solo lo consulta staff ---

def test_perm_031_un_anonimo_no_consulta_el_registro(spec_request):
    """
    SPEC:PERM-031 — El registro de actividad solo lo consulta staff
    Case: un anónimo no consulta el registro
    """
    response = spec_request(
        method='GET',
        path='/api/activity-events/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-031',
        case_name='un anónimo no consulta el registro',
        expected_status=None,
    )

def test_perm_031_un_usuario_autenticado_normal_no_consulta_el_registro(spec_request):
    """
    SPEC:PERM-031 — El registro de actividad solo lo consulta staff
    Case: un usuario autenticado normal no consulta el registro
    """
    response = spec_request(
        method='GET',
        path='/api/activity-events/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-031',
        case_name='un usuario autenticado normal no consulta el registro',
        expected_status=None,
    )

def test_perm_031_staff_consulta_el_registro(spec_request):
    """
    SPEC:PERM-031 — El registro de actividad solo lo consulta staff
    Case: staff consulta el registro
    """
    response = spec_request(
        method='GET',
        path='/api/activity-events/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-031',
        case_name='staff consulta el registro',
        expected_status=200,
    )


# --- PERM-032: El inicio de sesión es público ---

def test_perm_032_un_anonimo_inicia_sesion_con_credenciales_validas(spec_request):
    """
    SPEC:PERM-032 — El inicio de sesión es público
    Case: un anónimo inicia sesión con credenciales válidas
    """
    response = spec_request(
        method='POST',
        path='/api/login/',
        role='anonymous',
        given=None,
        body={'email': '{owner_email}', 'password': '{password}'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-032',
        case_name='un anónimo inicia sesión con credenciales válidas',
        expected_status=200,
    )


# --- PERM-034: El registro es público y la cuenta nace inactiva ---

def test_perm_034_un_anonimo_se_registra(spec_request):
    """
    SPEC:PERM-034 — El registro es público y la cuenta nace inactiva
    Case: un anónimo se registra
    """
    response = spec_request(
        method='POST',
        path='/api/register/',
        role='anonymous',
        given=None,
        body={'username': 'spec_registro', 'email': 'spec-registro@example.com', 'first_name': 'Spec', 'last_name': 'Registro', 'password': 'ClaveMuySegura789!'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-034',
        case_name='un anónimo se registra',
        expected_status=201,
    )


# --- PERM-035: El inicio de sesión con Google exige un correo verificado por Google ---

def test_perm_035_un_anonimo_alcanza_el_endpoint_de_google(spec_request):
    """
    SPEC:PERM-035 — El inicio de sesión con Google exige un correo verificado por Google
    Case: un anónimo alcanza el endpoint de Google
    """
    response = spec_request(
        method='POST',
        path='/api/auth/google/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-035',
        case_name='un anónimo alcanza el endpoint de Google',
        expected_status=None,
    )


# --- PERM-036: El refresco de token es público ---

def test_perm_036_un_anonimo_alcanza_el_refresco_de_token(spec_request):
    """
    SPEC:PERM-036 — El refresco de token es público
    Case: un anónimo alcanza el refresco de token
    """
    response = spec_request(
        method='POST',
        path='/api/token/refresh/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-036',
        case_name='un anónimo alcanza el refresco de token',
        expected_status=None,
    )


# --- PERM-037: La verificación de correo es pública ---

def test_perm_037_un_anonimo_alcanza_la_verificacion(spec_request):
    """
    SPEC:PERM-037 — La verificación de correo es pública
    Case: un anónimo alcanza la verificación
    """
    response = spec_request(
        method='POST',
        path='/api/verify-email/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-037',
        case_name='un anónimo alcanza la verificación',
        expected_status=None,
    )


# --- PERM-038: La recuperación de contraseña no revela si el correo existe ---

def test_perm_038_un_correo_inexistente_recibe_la_misma_respuesta_afirmativa(spec_request):
    """
    SPEC:PERM-038 — La recuperación de contraseña no revela si el correo existe
    Case: un correo inexistente recibe la misma respuesta afirmativa
    """
    response = spec_request(
        method='POST',
        path='/api/request-password-reset/',
        role='anonymous',
        given=None,
        body={'email': 'nadie-spec@example.com'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-038',
        case_name='un correo inexistente recibe la misma respuesta afirmativa',
        expected_status=200,
    )


# --- PERM-039: El reenvío de verificación revela si el correo existe ---

def test_perm_039_un_correo_inexistente_recibe_404_en_lugar_de_una_respuesta_generica(spec_request):
    """
    SPEC:PERM-039 — El reenvío de verificación revela si el correo existe
    Case: un correo inexistente recibe 404 en lugar de una respuesta genérica
    """
    response = spec_request(
        method='POST',
        path='/api/resend-verification/',
        role='anonymous',
        given=None,
        body={'email': 'nadie-spec@example.com'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=404,
        rule_id='PERM-039',
        case_name='un correo inexistente recibe 404 en lugar de una respuesta genérica',
        expected_status=None,
    )


# --- PERM-040: El reenvío de verificación debería responder siempre lo mismo ---

@pytest.mark.skip(reason="PERM-040 is 'proposed': no code implements it yet")
def test_perm_040_un_correo_inexistente_deberia_recibir_200_generico(spec_request):
    """
    SPEC:PERM-040 — El reenvío de verificación debería responder siempre lo mismo
    Case: un correo inexistente debería recibir 200 genérico
    """
    response = spec_request(
        method='POST',
        path='/api/resend-verification/',
        role='anonymous',
        given=None,
        body={'email': 'nadie-spec@example.com'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-040',
        case_name='un correo inexistente debería recibir 200 genérico',
        expected_status=200,
    )


# --- PERM-041: El perfil propio exige sesión y se resuelve desde el token ---

def test_perm_041_un_anonimo_no_puede_leer_un_perfil(spec_request):
    """
    SPEC:PERM-041 — El perfil propio exige sesión y se resuelve desde el token
    Case: un anónimo no puede leer un perfil
    """
    response = spec_request(
        method='GET',
        path='/api/me/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-041',
        case_name='un anónimo no puede leer un perfil',
        expected_status=None,
    )

def test_perm_041_un_usuario_autenticado_lee_su_perfil(spec_request):
    """
    SPEC:PERM-041 — El perfil propio exige sesión y se resuelve desde el token
    Case: un usuario autenticado lee su perfil
    """
    response = spec_request(
        method='GET',
        path='/api/me/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-041',
        case_name='un usuario autenticado lee su perfil',
        expected_status=200,
    )

def test_perm_041_staff_lee_su_propio_perfil(spec_request):
    """
    SPEC:PERM-041 — El perfil propio exige sesión y se resuelve desde el token
    Case: staff lee su propio perfil
    """
    response = spec_request(
        method='GET',
        path='/api/me/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-041',
        case_name='staff lee su propio perfil',
        expected_status=200,
    )


# --- PERM-042: Un usuario no puede concederse is_staff editando su perfil ---

def test_perm_042_un_anonimo_no_puede_modificar_ningun_perfil(spec_request):
    """
    SPEC:PERM-042 — Un usuario no puede concederse is_staff editando su perfil
    Case: un anónimo no puede modificar ningún perfil
    """
    response = spec_request(
        method='PATCH',
        path='/api/me/',
        role='anonymous',
        given=None,
        body={'first_name': 'Spec'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-042',
        case_name='un anónimo no puede modificar ningún perfil',
        expected_status=None,
    )

def test_perm_042_un_usuario_autenticado_edita_su_nombre_e_is_staff_se_ignora(spec_request):
    """
    SPEC:PERM-042 — Un usuario no puede concederse is_staff editando su perfil
    Case: un usuario autenticado edita su nombre e is_staff se ignora
    """
    response = spec_request(
        method='PATCH',
        path='/api/me/',
        role='authenticated',
        given=None,
        body={'first_name': 'Spec', 'is_staff': True},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-042',
        case_name='un usuario autenticado edita su nombre e is_staff se ignora',
        expected_status=200,
    )


# --- PERM-043: Cambiar la contraseña exige sesión y la contraseña actual ---

def test_perm_043_un_anonimo_no_puede_cambiar_contrasenas(spec_request):
    """
    SPEC:PERM-043 — Cambiar la contraseña exige sesión y la contraseña actual
    Case: un anónimo no puede cambiar contraseñas
    """
    response = spec_request(
        method='POST',
        path='/api/change-password/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-043',
        case_name='un anónimo no puede cambiar contraseñas',
        expected_status=None,
    )

def test_perm_043_un_usuario_autenticado_cambia_su_contrasena(spec_request):
    """
    SPEC:PERM-043 — Cambiar la contraseña exige sesión y la contraseña actual
    Case: un usuario autenticado cambia su contraseña
    """
    response = spec_request(
        method='POST',
        path='/api/change-password/',
        role='authenticated',
        given=None,
        body={'old_password': '{password}', 'new_password': 'OtraClaveSegura456!'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-043',
        case_name='un usuario autenticado cambia su contraseña',
        expected_status=200,
    )


# --- PERM-044: Cambiar el correo de la cuenta exige sesión ---

def test_perm_044_un_anonimo_no_puede_pedir_el_cambio_de_correo(spec_request):
    """
    SPEC:PERM-044 — Cambiar el correo de la cuenta exige sesión
    Case: un anónimo no puede pedir el cambio de correo
    """
    response = spec_request(
        method='POST',
        path='/api/request-email-change/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-044',
        case_name='un anónimo no puede pedir el cambio de correo',
        expected_status=None,
    )

def test_perm_044_un_usuario_autenticado_pide_el_cambio_de_correo(spec_request):
    """
    SPEC:PERM-044 — Cambiar el correo de la cuenta exige sesión
    Case: un usuario autenticado pide el cambio de correo
    """
    response = spec_request(
        method='POST',
        path='/api/request-email-change/',
        role='authenticated',
        given=None,
        body={'new_email': 'spec-nuevo@example.com'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=401,
        rule_id='PERM-044',
        case_name='un usuario autenticado pide el cambio de correo',
        expected_status=200,
    )


# --- PERM-045: El dashboard de métricas es solo para staff ---

def test_perm_045_un_anonimo_no_accede_al_dashboard(spec_request):
    """
    SPEC:PERM-045 — El dashboard de métricas es solo para staff
    Case: un anónimo no accede al dashboard
    """
    response = spec_request(
        method='GET',
        path='/api/admin/dashboard/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-045',
        case_name='un anónimo no accede al dashboard',
        expected_status=None,
    )

def test_perm_045_un_usuario_autenticado_normal_no_accede_al_dashboard(spec_request):
    """
    SPEC:PERM-045 — El dashboard de métricas es solo para staff
    Case: un usuario autenticado normal no accede al dashboard
    """
    response = spec_request(
        method='GET',
        path='/api/admin/dashboard/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-045',
        case_name='un usuario autenticado normal no accede al dashboard',
        expected_status=None,
    )

def test_perm_045_staff_accede_al_dashboard(spec_request):
    """
    SPEC:PERM-045 — El dashboard de métricas es solo para staff
    Case: staff accede al dashboard
    """
    response = spec_request(
        method='GET',
        path='/api/admin/dashboard/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-045',
        case_name='staff accede al dashboard',
        expected_status=200,
    )

def test_perm_045_un_superusuario_accede_al_dashboard(spec_request):
    """
    SPEC:PERM-045 — El dashboard de métricas es solo para staff
    Case: un superusuario accede al dashboard
    """
    response = spec_request(
        method='GET',
        path='/api/admin/dashboard/',
        role='superuser',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-045',
        case_name='un superusuario accede al dashboard',
        expected_status=200,
    )


# --- PERM-046: El estado del sistema es solo para staff ---

def test_perm_046_un_anonimo_no_accede_al_estado_del_sistema(spec_request):
    """
    SPEC:PERM-046 — El estado del sistema es solo para staff
    Case: un anónimo no accede al estado del sistema
    """
    response = spec_request(
        method='GET',
        path='/api/admin/system-status/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-046',
        case_name='un anónimo no accede al estado del sistema',
        expected_status=None,
    )

def test_perm_046_un_usuario_autenticado_normal_no_accede(spec_request):
    """
    SPEC:PERM-046 — El estado del sistema es solo para staff
    Case: un usuario autenticado normal no accede
    """
    response = spec_request(
        method='GET',
        path='/api/admin/system-status/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-046',
        case_name='un usuario autenticado normal no accede',
        expected_status=None,
    )

def test_perm_046_staff_accede_al_estado_del_sistema(spec_request):
    """
    SPEC:PERM-046 — El estado del sistema es solo para staff
    Case: staff accede al estado del sistema
    """
    response = spec_request(
        method='GET',
        path='/api/admin/system-status/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-046',
        case_name='staff accede al estado del sistema',
        expected_status=200,
    )


# --- PERM-047: Resolver una incidencia del sistema exige staff ---

def test_perm_047_un_anonimo_no_puede_resolver_incidencias(spec_request):
    """
    SPEC:PERM-047 — Resolver una incidencia del sistema exige staff
    Case: un anónimo no puede resolver incidencias
    """
    response = spec_request(
        method='POST',
        path='/api/admin/system-status/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-047',
        case_name='un anónimo no puede resolver incidencias',
        expected_status=None,
    )

def test_perm_047_un_usuario_autenticado_normal_no_puede_resolver_incidencias(spec_request):
    """
    SPEC:PERM-047 — Resolver una incidencia del sistema exige staff
    Case: un usuario autenticado normal no puede resolver incidencias
    """
    response = spec_request(
        method='POST',
        path='/api/admin/system-status/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-047',
        case_name='un usuario autenticado normal no puede resolver incidencias',
        expected_status=None,
    )

def test_perm_047_staff_supera_la_barrera_de_permisos(spec_request):
    """
    SPEC:PERM-047 — Resolver una incidencia del sistema exige staff
    Case: staff supera la barrera de permisos
    """
    response = spec_request(
        method='POST',
        path='/api/admin/system-status/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-047',
        case_name='staff supera la barrera de permisos',
        expected_status=None,
    )


# --- PERM-048: La gestión de usuarios del panel es solo para staff ---

def test_perm_048_un_anonimo_no_lista_usuarios(spec_request):
    """
    SPEC:PERM-048 — La gestión de usuarios del panel es solo para staff
    Case: un anónimo no lista usuarios
    """
    response = spec_request(
        method='GET',
        path='/api/admin/users/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-048',
        case_name='un anónimo no lista usuarios',
        expected_status=None,
    )

def test_perm_048_un_usuario_autenticado_normal_no_lista_usuarios(spec_request):
    """
    SPEC:PERM-048 — La gestión de usuarios del panel es solo para staff
    Case: un usuario autenticado normal no lista usuarios
    """
    response = spec_request(
        method='GET',
        path='/api/admin/users/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-048',
        case_name='un usuario autenticado normal no lista usuarios',
        expected_status=None,
    )

def test_perm_048_staff_lista_usuarios(spec_request):
    """
    SPEC:PERM-048 — La gestión de usuarios del panel es solo para staff
    Case: staff lista usuarios
    """
    response = spec_request(
        method='GET',
        path='/api/admin/users/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-048',
        case_name='staff lista usuarios',
        expected_status=200,
    )


# --- PERM-049: Cualquier staff puede conceder is_staff a otra cuenta ---

def test_perm_049_un_anonimo_no_puede_modificar_cuentas(spec_request):
    """
    SPEC:PERM-049 — Cualquier staff puede conceder is_staff a otra cuenta
    Case: un anónimo no puede modificar cuentas
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/users/{owner_id}/',
        role='anonymous',
        given=None,
        body={'is_staff': True},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-049',
        case_name='un anónimo no puede modificar cuentas',
        expected_status=None,
    )

def test_perm_049_un_usuario_autenticado_normal_no_puede_modificar_cuentas(spec_request):
    """
    SPEC:PERM-049 — Cualquier staff puede conceder is_staff a otra cuenta
    Case: un usuario autenticado normal no puede modificar cuentas
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/users/{owner_id}/',
        role='authenticated',
        given=None,
        body={'is_staff': True},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-049',
        case_name='un usuario autenticado normal no puede modificar cuentas',
        expected_status=None,
    )

def test_perm_049_un_staff_promueve_a_otra_cuenta_a_staff_sin_ser_superusuario(spec_request):
    """
    SPEC:PERM-049 — Cualquier staff puede conceder is_staff a otra cuenta
    Case: un staff promueve a otra cuenta a staff sin ser superusuario
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/users/{owner_id}/',
        role='staff',
        given=None,
        body={'is_staff': True},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-049',
        case_name='un staff promueve a otra cuenta a staff sin ser superusuario',
        expected_status=200,
    )


# --- PERM-050: Conceder is_staff debería exigir is_superuser ---

@pytest.mark.skip(reason="PERM-050 is 'proposed': no code implements it yet")
def test_perm_050_un_staff_normal_no_deberia_poder_conceder_is_staff(spec_request):
    """
    SPEC:PERM-050 — Conceder is_staff debería exigir is_superuser
    Case: un staff normal no debería poder conceder is_staff
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/users/{owner_id}/',
        role='staff',
        given=None,
        body={'is_staff': True},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-050',
        case_name='un staff normal no debería poder conceder is_staff',
        expected_status=None,
    )

@pytest.mark.skip(reason="PERM-050 is 'proposed': no code implements it yet")
def test_perm_050_un_superusuario_si_deberia_poder(spec_request):
    """
    SPEC:PERM-050 — Conceder is_staff debería exigir is_superuser
    Case: un superusuario sí debería poder
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/users/{owner_id}/',
        role='superuser',
        given=None,
        body={'is_staff': True},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-050',
        case_name='un superusuario sí debería poder',
        expected_status=200,
    )


# --- PERM-051: Cualquier staff puede eliminar cuentas de usuario ---

def test_perm_051_un_anonimo_no_puede_borrar_cuentas(spec_request):
    """
    SPEC:PERM-051 — Cualquier staff puede eliminar cuentas de usuario
    Case: un anónimo no puede borrar cuentas
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/users/{owner_id}/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-051',
        case_name='un anónimo no puede borrar cuentas',
        expected_status=None,
    )

def test_perm_051_un_usuario_autenticado_normal_no_puede_borrar_cuentas(spec_request):
    """
    SPEC:PERM-051 — Cualquier staff puede eliminar cuentas de usuario
    Case: un usuario autenticado normal no puede borrar cuentas
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/users/{owner_id}/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-051',
        case_name='un usuario autenticado normal no puede borrar cuentas',
        expected_status=None,
    )

def test_perm_051_un_staff_borra_una_cuenta_ajena_sin_ser_superusuario(spec_request):
    """
    SPEC:PERM-051 — Cualquier staff puede eliminar cuentas de usuario
    Case: un staff borra una cuenta ajena sin ser superusuario
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/users/{owner_id}/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-051',
        case_name='un staff borra una cuenta ajena sin ser superusuario',
        expected_status=204,
    )


# --- PERM-052: Eliminar cuentas debería exigir is_superuser ---

@pytest.mark.skip(reason="PERM-052 is 'proposed': no code implements it yet")
def test_perm_052_un_staff_normal_no_deberia_poder_borrar_cuentas(spec_request):
    """
    SPEC:PERM-052 — Eliminar cuentas debería exigir is_superuser
    Case: un staff normal no debería poder borrar cuentas
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/users/{owner_id}/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-052',
        case_name='un staff normal no debería poder borrar cuentas',
        expected_status=None,
    )

@pytest.mark.skip(reason="PERM-052 is 'proposed': no code implements it yet")
def test_perm_052_un_superusuario_si_deberia_poder(spec_request):
    """
    SPEC:PERM-052 — Eliminar cuentas debería exigir is_superuser
    Case: un superusuario sí debería poder
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/users/{owner_id}/',
        role='superuser',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-052',
        case_name='un superusuario sí debería poder',
        expected_status=204,
    )


# --- PERM-053: El listado de propiedades del panel es solo para staff ---

def test_perm_053_un_anonimo_no_lista_el_inventario_del_panel(spec_request):
    """
    SPEC:PERM-053 — El listado de propiedades del panel es solo para staff
    Case: un anónimo no lista el inventario del panel
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-053',
        case_name='un anónimo no lista el inventario del panel',
        expected_status=None,
    )

def test_perm_053_un_usuario_autenticado_normal_no_lista_el_inventario_del_panel(spec_request):
    """
    SPEC:PERM-053 — El listado de propiedades del panel es solo para staff
    Case: un usuario autenticado normal no lista el inventario del panel
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-053',
        case_name='un usuario autenticado normal no lista el inventario del panel',
        expected_status=None,
    )

def test_perm_053_el_propietario_tampoco_accede_al_panel(spec_request):
    """
    SPEC:PERM-053 — El listado de propiedades del panel es solo para staff
    Case: el propietario tampoco accede al panel
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-053',
        case_name='el propietario tampoco accede al panel',
        expected_status=None,
    )

def test_perm_053_staff_lista_el_inventario_completo(spec_request):
    """
    SPEC:PERM-053 — El listado de propiedades del panel es solo para staff
    Case: staff lista el inventario completo
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-053',
        case_name='staff lista el inventario completo',
        expected_status=200,
    )


# --- PERM-054: Los contadores de propiedades del panel son solo para staff ---

def test_perm_054_un_usuario_autenticado_normal_no_ve_los_contadores(spec_request):
    """
    SPEC:PERM-054 — Los contadores de propiedades del panel son solo para staff
    Case: un usuario autenticado normal no ve los contadores
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/stats/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-054',
        case_name='un usuario autenticado normal no ve los contadores',
        expected_status=None,
    )

def test_perm_054_staff_ve_los_contadores(spec_request):
    """
    SPEC:PERM-054 — Los contadores de propiedades del panel son solo para staff
    Case: staff ve los contadores
    """
    response = spec_request(
        method='GET',
        path='/api/admin/properties/stats/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-054',
        case_name='staff ve los contadores',
        expected_status=200,
    )


# --- PERM-055: La edición administrativa de propiedades está acotada a cinco campos ---

def test_perm_055_un_anonimo_no_puede_moderar_propiedades(spec_request):
    """
    SPEC:PERM-055 — La edición administrativa de propiedades está acotada a cinco campos
    Case: un anónimo no puede moderar propiedades
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/properties/{property_id}/',
        role='anonymous',
        given=None,
        body={'status': 'inactive'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-055',
        case_name='un anónimo no puede moderar propiedades',
        expected_status=None,
    )

def test_perm_055_el_propietario_no_puede_usar_la_ruta_administrativa(spec_request):
    """
    SPEC:PERM-055 — La edición administrativa de propiedades está acotada a cinco campos
    Case: el propietario no puede usar la ruta administrativa
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/properties/{property_id}/',
        role='owner',
        given=None,
        body={'status': 'inactive'},
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-055',
        case_name='el propietario no puede usar la ruta administrativa',
        expected_status=None,
    )

def test_perm_055_staff_modera_una_propiedad_ajena(spec_request):
    """
    SPEC:PERM-055 — La edición administrativa de propiedades está acotada a cinco campos
    Case: staff modera una propiedad ajena
    """
    response = spec_request(
        method='PATCH',
        path='/api/admin/properties/{property_id}/',
        role='staff',
        given=None,
        body={'status': 'inactive'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-055',
        case_name='staff modera una propiedad ajena',
        expected_status=200,
    )


# --- PERM-056: El borrado administrativo de propiedades es solo para staff ---

def test_perm_056_un_usuario_autenticado_normal_no_puede_borrar_por_la_ruta_administrativa(spec_request):
    """
    SPEC:PERM-056 — El borrado administrativo de propiedades es solo para staff
    Case: un usuario autenticado normal no puede borrar por la ruta administrativa
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/properties/{property_id}/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-056',
        case_name='un usuario autenticado normal no puede borrar por la ruta administrativa',
        expected_status=None,
    )

def test_perm_056_staff_borra_una_propiedad_ajena(spec_request):
    """
    SPEC:PERM-056 — El borrado administrativo de propiedades es solo para staff
    Case: staff borra una propiedad ajena
    """
    response = spec_request(
        method='DELETE',
        path='/api/admin/properties/{property_id}/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-056',
        case_name='staff borra una propiedad ajena',
        expected_status=204,
    )


# --- PERM-057: El cambio masivo de estado es solo para staff y está acotado ---

def test_perm_057_un_anonimo_no_puede_cambiar_estados_en_masa(spec_request):
    """
    SPEC:PERM-057 — El cambio masivo de estado es solo para staff y está acotado
    Case: un anónimo no puede cambiar estados en masa
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/bulk-status/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-057',
        case_name='un anónimo no puede cambiar estados en masa',
        expected_status=None,
    )

def test_perm_057_un_usuario_autenticado_normal_no_puede_cambiar_estados_en_masa(spec_request):
    """
    SPEC:PERM-057 — El cambio masivo de estado es solo para staff y está acotado
    Case: un usuario autenticado normal no puede cambiar estados en masa
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/bulk-status/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-057',
        case_name='un usuario autenticado normal no puede cambiar estados en masa',
        expected_status=None,
    )

def test_perm_057_staff_cambia_el_estado_de_un_lote_valido(spec_request):
    """
    SPEC:PERM-057 — El cambio masivo de estado es solo para staff y está acotado
    Case: staff cambia el estado de un lote válido
    """
    response = spec_request(
        method='POST',
        path='/api/admin/properties/bulk-status/',
        role='staff',
        given=None,
        body={'ids': ['{property_id}'], 'status': 'inactive'},
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-057',
        case_name='staff cambia el estado de un lote válido',
        expected_status=200,
    )


# --- PERM-058: Las fuentes de ingesta solo las consulta staff ---

def test_perm_058_un_anonimo_no_consulta_las_fuentes(spec_request):
    """
    SPEC:PERM-058 — Las fuentes de ingesta solo las consulta staff
    Case: un anónimo no consulta las fuentes
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/sources/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-058',
        case_name='un anónimo no consulta las fuentes',
        expected_status=None,
    )

def test_perm_058_un_usuario_autenticado_normal_no_consulta_las_fuentes(spec_request):
    """
    SPEC:PERM-058 — Las fuentes de ingesta solo las consulta staff
    Case: un usuario autenticado normal no consulta las fuentes
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/sources/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-058',
        case_name='un usuario autenticado normal no consulta las fuentes',
        expected_status=None,
    )

def test_perm_058_staff_consulta_las_fuentes(spec_request):
    """
    SPEC:PERM-058 — Las fuentes de ingesta solo las consulta staff
    Case: staff consulta las fuentes
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/sources/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-058',
        case_name='staff consulta las fuentes',
        expected_status=200,
    )


# --- PERM-059: Las ejecuciones de ingesta solo las consulta staff ---

def test_perm_059_un_anonimo_no_consulta_las_ejecuciones(spec_request):
    """
    SPEC:PERM-059 — Las ejecuciones de ingesta solo las consulta staff
    Case: un anónimo no consulta las ejecuciones
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/runs/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-059',
        case_name='un anónimo no consulta las ejecuciones',
        expected_status=None,
    )

def test_perm_059_un_usuario_autenticado_normal_no_consulta_las_ejecuciones(spec_request):
    """
    SPEC:PERM-059 — Las ejecuciones de ingesta solo las consulta staff
    Case: un usuario autenticado normal no consulta las ejecuciones
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/runs/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-059',
        case_name='un usuario autenticado normal no consulta las ejecuciones',
        expected_status=None,
    )

def test_perm_059_staff_consulta_las_ejecuciones(spec_request):
    """
    SPEC:PERM-059 — Las ejecuciones de ingesta solo las consulta staff
    Case: staff consulta las ejecuciones
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/runs/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-059',
        case_name='staff consulta las ejecuciones',
        expected_status=200,
    )


# --- PERM-060: Los anuncios importados solo los revisa staff ---

def test_perm_060_un_anonimo_no_revisa_los_anuncios_importados(spec_request):
    """
    SPEC:PERM-060 — Los anuncios importados solo los revisa staff
    Case: un anónimo no revisa los anuncios importados
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/properties/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-060',
        case_name='un anónimo no revisa los anuncios importados',
        expected_status=None,
    )

def test_perm_060_un_usuario_autenticado_normal_no_los_revisa(spec_request):
    """
    SPEC:PERM-060 — Los anuncios importados solo los revisa staff
    Case: un usuario autenticado normal no los revisa
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/properties/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-060',
        case_name='un usuario autenticado normal no los revisa',
        expected_status=None,
    )

def test_perm_060_staff_supera_la_barrera_de_permisos(spec_request):
    """
    SPEC:PERM-060 — Los anuncios importados solo los revisa staff
    Case: staff supera la barrera de permisos
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/properties/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-060',
        case_name='staff supera la barrera de permisos',
        expected_status=None,
    )


# --- PERM-061: Lanzar una ingesta exige staff ---

def test_perm_061_un_anonimo_no_puede_lanzar_una_ingesta(spec_request):
    """
    SPEC:PERM-061 — Lanzar una ingesta exige staff
    Case: un anónimo no puede lanzar una ingesta
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/launch/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-061',
        case_name='un anónimo no puede lanzar una ingesta',
        expected_status=None,
    )

def test_perm_061_un_usuario_autenticado_normal_no_puede_lanzar_una_ingesta(spec_request):
    """
    SPEC:PERM-061 — Lanzar una ingesta exige staff
    Case: un usuario autenticado normal no puede lanzar una ingesta
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/launch/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-061',
        case_name='un usuario autenticado normal no puede lanzar una ingesta',
        expected_status=None,
    )

def test_perm_061_el_propietario_de_propiedades_tampoco_puede(spec_request):
    """
    SPEC:PERM-061 — Lanzar una ingesta exige staff
    Case: el propietario de propiedades tampoco puede
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/launch/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-061',
        case_name='el propietario de propiedades tampoco puede',
        expected_status=None,
    )


# --- PERM-062: Cancelar una ejecución exige staff ---

def test_perm_062_un_anonimo_no_puede_cancelar_ejecuciones(spec_request):
    """
    SPEC:PERM-062 — Cancelar una ejecución exige staff
    Case: un anónimo no puede cancelar ejecuciones
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/cancel/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-062',
        case_name='un anónimo no puede cancelar ejecuciones',
        expected_status=None,
    )

def test_perm_062_un_usuario_autenticado_normal_no_puede_cancelar_ejecuciones(spec_request):
    """
    SPEC:PERM-062 — Cancelar una ejecución exige staff
    Case: un usuario autenticado normal no puede cancelar ejecuciones
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/cancel/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-062',
        case_name='un usuario autenticado normal no puede cancelar ejecuciones',
        expected_status=None,
    )

def test_perm_062_staff_supera_la_barrera_de_permisos(spec_request):
    """
    SPEC:PERM-062 — Cancelar una ejecución exige staff
    Case: staff supera la barrera de permisos
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/cancel/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-062',
        case_name='staff supera la barrera de permisos',
        expected_status=None,
    )


# --- PERM-063: Re-scrapear un anuncio exige staff ---

def test_perm_063_un_anonimo_no_puede_re_scrapear(spec_request):
    """
    SPEC:PERM-063 — Re-scrapear un anuncio exige staff
    Case: un anónimo no puede re-scrapear
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/refresh-property/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-063',
        case_name='un anónimo no puede re-scrapear',
        expected_status=None,
    )

def test_perm_063_un_usuario_autenticado_normal_no_puede_re_scrapear(spec_request):
    """
    SPEC:PERM-063 — Re-scrapear un anuncio exige staff
    Case: un usuario autenticado normal no puede re-scrapear
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/refresh-property/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-063',
        case_name='un usuario autenticado normal no puede re-scrapear',
        expected_status=None,
    )


# --- PERM-064: La previsualización de mantenimiento exige staff y no modifica nada ---

def test_perm_064_un_anonimo_no_previsualiza_el_mantenimiento(spec_request):
    """
    SPEC:PERM-064 — La previsualización de mantenimiento exige staff y no modifica nada
    Case: un anónimo no previsualiza el mantenimiento
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/maintenance/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-064',
        case_name='un anónimo no previsualiza el mantenimiento',
        expected_status=None,
    )

def test_perm_064_un_usuario_autenticado_normal_no_lo_previsualiza(spec_request):
    """
    SPEC:PERM-064 — La previsualización de mantenimiento exige staff y no modifica nada
    Case: un usuario autenticado normal no lo previsualiza
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/maintenance/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-064',
        case_name='un usuario autenticado normal no lo previsualiza',
        expected_status=None,
    )

def test_perm_064_staff_previsualiza_los_candidatos(spec_request):
    """
    SPEC:PERM-064 — La previsualización de mantenimiento exige staff y no modifica nada
    Case: staff previsualiza los candidatos
    """
    response = spec_request(
        method='GET',
        path='/api/admin/ingesta/maintenance/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-064',
        case_name='staff previsualiza los candidatos',
        expected_status=200,
    )


# --- PERM-065: La limpieza de importados exige staff, confirmación literal y lote acotado ---

def test_perm_065_un_anonimo_no_puede_lanzar_la_limpieza(spec_request):
    """
    SPEC:PERM-065 — La limpieza de importados exige staff, confirmación literal y lote acotado
    Case: un anónimo no puede lanzar la limpieza
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/maintenance/cleanup/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-065',
        case_name='un anónimo no puede lanzar la limpieza',
        expected_status=None,
    )

def test_perm_065_un_usuario_autenticado_normal_no_puede_lanzar_la_limpieza(spec_request):
    """
    SPEC:PERM-065 — La limpieza de importados exige staff, confirmación literal y lote acotado
    Case: un usuario autenticado normal no puede lanzar la limpieza
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/maintenance/cleanup/',
        role='authenticated',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-065',
        case_name='un usuario autenticado normal no puede lanzar la limpieza',
        expected_status=None,
    )

def test_perm_065_el_propietario_de_propiedades_tampoco_puede(spec_request):
    """
    SPEC:PERM-065 — La limpieza de importados exige staff, confirmación literal y lote acotado
    Case: el propietario de propiedades tampoco puede
    """
    response = spec_request(
        method='POST',
        path='/api/admin/ingesta/maintenance/cleanup/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-065',
        case_name='el propietario de propiedades tampoco puede',
        expected_status=None,
    )


# --- PERM-066: La sonda de salud es pública ---

def test_perm_066_un_anonimo_consulta_la_salud_del_sistema(spec_request):
    """
    SPEC:PERM-066 — La sonda de salud es pública
    Case: un anónimo consulta la salud del sistema
    """
    response = spec_request(
        method='GET',
        path='/api/health/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=None,
        rule_id='PERM-066',
        case_name='un anónimo consulta la salud del sistema',
        expected_status=200,
    )


# --- PERM-071: El informe de promoción es del dueño del anuncio ---

def test_perm_071_el_propietario_consulta_su_informe(spec_request):
    """
    SPEC:PERM-071 — El informe de promoción es del dueño del anuncio
    Case: el propietario consulta su informe
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/promotion-stats/',
        role='owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-071',
        case_name='el propietario consulta su informe',
        expected_status=200,
    )

def test_perm_071_un_tercero_autenticado_no_lo_ve(spec_request):
    """
    SPEC:PERM-071 — El informe de promoción es del dueño del anuncio
    Case: un tercero autenticado no lo ve
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/promotion-stats/',
        role='not_owner',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=403,
        rule_id='PERM-071',
        case_name='un tercero autenticado no lo ve',
        expected_status=None,
    )

def test_perm_071_un_anonimo_no_lo_ve(spec_request):
    """
    SPEC:PERM-071 — El informe de promoción es del dueño del anuncio
    Case: un anónimo no lo ve
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/promotion-stats/',
        role='anonymous',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='denied',
        denied_status=401,
        rule_id='PERM-071',
        case_name='un anónimo no lo ve',
        expected_status=None,
    )

def test_perm_071_staff_lo_consulta_de_cualquier_anuncio(spec_request):
    """
    SPEC:PERM-071 — El informe de promoción es del dueño del anuncio
    Case: staff lo consulta de cualquier anuncio
    """
    response = spec_request(
        method='GET',
        path='/api/properties/{property_id}/promotion-stats/',
        role='staff',
        given=None,
        body=None,
    )
    assert_outcome(
        response,
        expected='allowed',
        denied_status=403,
        rule_id='PERM-071',
        case_name='staff lo consulta de cualquier anuncio',
        expected_status=200,
    )
