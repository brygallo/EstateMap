# Production Environment Variables
# Este archivo es generado automáticamente por ./scripts/deploy.sh
# Si necesitas crearlo manualmente, copia este archivo como .env.prod y completa los valores

# Database
DB_PASSWORD=your_secure_database_password

# MinIO
MINIO_ROOT_PASSWORD=your_secure_minio_password

# Django
DJANGO_SECRET_KEY=your_secure_django_secret_key
ALLOWED_HOSTS=your_server_ip,localhost,127.0.0.1

# Server (auto-detectado por deploy.sh)
SERVER_IP=your_server_ip
