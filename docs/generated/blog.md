<!-- GENERATED BY tools/specs — DO NOT EDIT BY HAND -->
<!-- Fuente: specs/domains/blog.yaml -->

# Blog editorial

> Generado desde `specs/domains/blog.yaml`. **No edites este archivo**: edita el YAML y ejecuta `./scripts/specs.sh docs`.

**Estado del dominio:** ✅ Implementada

El portal publica artículos escritos por el equipo desde el admin de Django y servidos por Next.js en `/blog`. Existe por una razón concreta: el inventario de anuncios posiciona consultas transaccionales («departamentos en venta en Cumbayá») pero no las informativas («cuánto cuesta la alcabala», «qué exige la Ley de Inquilinato»), que son las que la gente pregunta antes de comprar y las que los buscadores de IA citan.
La pieza que da forma al dominio es la programación. Escribir treinta artículos en una tarde y soltarlos el mismo día se lee como lo que es; el calendario los reparte uno por día. Por eso la publicación se decide con una fecha, no con una cola: un post es público desde que `published_at` queda en el pasado, aunque el worker esté caído. Celery solo se encarga de los efectos (avisar a IndexNow, invalidar el caché de Next), que llegar tarde encarece pero no rompe.
La API pública es de solo lectura. La escritura se realiza desde el escritorio editorial protegido del panel de administración. Las siete guías que vivían incrustadas en el frontend (`lib/guias.ts`) son sus primeros posts y conservan su slug, con `/guias/<slug>` redirigiendo de forma permanente a `/blog/<slug>`.

**Ver también:** `specs/ui/visibility-rules.yaml`, `specs/permissions/matrix.yaml`, `docs/technical/cache.md`, `docs/seo/editorial-y-backlinks.md`

## Reglas

| Id | Regla | Estado |
| --- | --- | --- |
| [`BLOG-001`](#blog-001--un-post-es-público-desde-que-su-fecha-de-publicación-queda-en-el-pasado) | Un post es público desde que su fecha de publicación queda en el pasado | ✅ Implementada |
| [`BLOG-002`](#blog-002--los-borradores-y-los-archivados-no-salen-nunca-al-público) | Los borradores y los archivados no salen nunca al público | ✅ Implementada |
| [`BLOG-003`](#blog-003--la-api-pública-del-blog-es-de-solo-lectura) | La API pública del blog es de solo lectura | ✅ Implementada |
| [`BLOG-010`](#blog-010--solo-administradores-gestionan-el-blog-desde-el-panel) | Solo administradores gestionan el blog desde el panel | ✅ Implementada |
| [`BLOG-011`](#blog-011--el-editor-controla-el-ciclo-editorial-completo) | El editor controla el ciclo editorial completo | ✅ Implementada |
| [`BLOG-004`](#blog-004--el-listado-público-entrega-como-mucho-60-posts-por-petición) | El listado público entrega como mucho 60 posts por petición | ✅ Implementada |
| [`BLOG-005`](#blog-005--publicar-avisa-a-los-buscadores-e-invalida-el-caché-del-frontend) | Publicar avisa a los buscadores e invalida el caché del frontend | ✅ Implementada |
| [`BLOG-006`](#blog-006--las-guías-migradas-conservan-su-url-mediante-redirección-permanente) | Las guías migradas conservan su URL mediante redirección permanente | ✅ Implementada |
| [`BLOG-007`](#blog-007--programar-un-lote-no-pisa-lo-que-ya-está-en-el-calendario) | Programar un lote no pisa lo que ya está en el calendario | ✅ Implementada |
| [`BLOG-008`](#blog-008--cada-autor-con-nombre-público-tiene-una-página-enlazable) | Cada autor con nombre público tiene una página enlazable | ✅ Implementada |
| [`BLOG-009`](#blog-009--un-artículo-de-ciudad-entrega-el-precio-del-m²-y-el-inventario-de-esa-ciudad) | Un artículo de ciudad entrega el precio del m² y el inventario de esa ciudad | ✅ Implementada |
| [`BLOG-012`](#blog-012--un-artículo-programado-no-declara-que-se-modificó-antes-de-publicarse) | Un artículo programado no declara que se modificó antes de publicarse | ✅ Implementada |
| [`BLOG-013`](#blog-013--un-artículo-con-cifras-escritas-a-mano-se-revisa-cada-trimestre) | Un artículo con cifras escritas a mano se revisa cada trimestre | ✅ Implementada |
| [`BLOG-014`](#blog-014--un-artículo-publicitario-se-identifica-donde-se-lee) | Un artículo publicitario se identifica donde se lee | ✅ Implementada |

### BLOG-001 — Un post es público desde que su fecha de publicación queda en el pasado

**Estado:** ✅ Implementada

La API pública entrega un post cuando `published_at` es anterior al instante actual, sin depender de que ninguna tarea lo haya marcado.

> **Por qué:** La alternativa evidente —que una tarea de Celery cambie el estado a `published` y solo entonces se vea— convierte al broker en autoridad sobre el calendario editorial. Una caída del worker un sábado congelaría la publicación hasta que alguien lo note, y lo notaría por el hueco en Search Console, no por una alerta.
Aquí la fecha manda y el estado la acompaña: el filtro público acepta `scheduled` y `published` por igual siempre que la fecha haya llegado. La tarea horaria sigue existiendo, pero solo para lo que un reloj no puede hacer: avisar a IndexNow y tirar el caché de Next. Si no corre, el post sale igual y tarda un poco más en ser indexado.

**Backend**

- Endpoint: `GET /api/blog/posts/`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/models.py:62-77` (`def public`) — El filtro público acepta SCHEDULED y PUBLISHED; lo que decide es `published_at__lte=timezone.now()`.
- `backend/blog/views.py:49-51` (`Post.objects.public()`) — Es el único queryset del que sale cualquier respuesta pública.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un post programado para ayer ya se lee | — | `blog_post_published_days_ago`=1 | — | visible |
| Un post programado para mañana todavía no se lee | — | `blog_post_published_in_days`=1 | — | ausente |

**Cobertura exigida:** api

- `backend/blog/tests/test_blog_api.py`

### BLOG-002 — Los borradores y los archivados no salen nunca al público

**Estado:** ✅ Implementada

Un post en estado `draft` o `archived` queda fuera del listado y devuelve 404 en su detalle, tenga la fecha que tenga.

> **Por qué:** El estado es la única forma de retirar del sitio algo ya publicado sin borrarlo ni inventar una fecha futura, que es lo que haría un editor apurado si no existiera. Se comprueba en el mismo queryset que BLOG-001 para que no haya dos definiciones de «público» que puedan divergir.

**Backend**

- Endpoint: `GET /api/blog/posts/`
- Al denegar: HTTP `404`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/models.py:62-77` (`status__in=(Post.Status.SCHEDULED, Post.Status.PUBLISHED)`) — DRAFT y ARCHIVED quedan fuera por construcción.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un borrador con fecha pasada sigue sin verse | — | `blog_post_draft`=sí | — | ausente |
| El detalle de un borrador devuelve 404 | — | `blog_post_draft`=sí | — | denied (HTTP 404) |

**Cobertura exigida:** api

- `backend/blog/tests/test_blog_api.py`

### BLOG-003 — La API pública del blog es de solo lectura

**Estado:** ✅ Implementada

La API pública del blog no expone crear, editar ni borrar a nadie. La escritura usa endpoints administrativos separados y protegidos.

> **Por qué:** Es contenido que edita un puñado de personas unas veces por semana. Abrir un endpoint de escritura para eso añade superficie de ataque, validación y permisos a cambio de comodidad que el admin de Django ya da. La regla se sostiene por el tipo de viewset, no por una comprobación que alguien pueda olvidar: `ReadOnlyModelViewSet` sencillamente no enruta POST ni PUT.

**Backend**

- Endpoint: `POST /api/blog/posts/`
- Al denegar: HTTP `405`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/views.py:40-44` (`class BlogPostViewSet(viewsets.ReadOnlyModelViewSet)`)
- `backend/blog/views.py:113-120` (`class BlogCategoryViewSet(viewsets.ReadOnlyModelViewSet)`)

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un anónimo no puede crear posts | anonymous | — | — | denied (HTTP 405) |
| Un usuario autenticado tampoco | authenticated | — | — | denied (HTTP 405) |

**Cobertura exigida:** api

- `backend/blog/tests/test_blog_api.py`
- `backend/real_estate/tests/generated/test_spec_blog.py`

### BLOG-010 — Solo administradores gestionan el blog desde el panel

**Estado:** ✅ Implementada

Los endpoints editoriales permiten listar, crear, editar, publicar, programar y eliminar contenido únicamente a cuentas con `is_staff`.

> **Por qué:** El JWT que lee el frontend sirve para adaptar la interfaz, pero la frontera de seguridad está en Django y comprueba el usuario autenticado.

**Backend**

- Endpoint: `POST /api/admin/blog/posts/`
- Al denegar: HTTP `403`

**Frontend**

- Ruta: `/admin/blog`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/admin_api.py` (`class AdminBlogPostViewSet`) — El viewset exige autenticación y el permiso de administrador.
- `frontend/app/admin/blog/page.tsx` (`export default function BlogAdminPage`) — El escritorio editorial vive dentro del panel principal.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un usuario normal no puede editar un artículo | authenticated | — | — | denied (HTTP 403) |
| Un administrador puede crear un borrador | staff | — | `title`=Cómo comprar una casa, `slug`=como-comprar-una-casa, `excerpt`=Una guía clara para preparar la compra., `body`=## Primer paso

Compara las alternativas., `status`=draft | allowed |

**Cobertura exigida:** api

- `backend/blog/tests/test_admin_api.py`
- `backend/real_estate/tests/generated/test_spec_blog.py`

### BLOG-011 — El editor controla el ciclo editorial completo

**Estado:** ✅ Implementada

Un artículo puede guardarse como borrador, programarse con fecha futura, publicarse inmediatamente o retirarse del sitio desde el panel integrado.

> **Por qué:** El panel expone el calendario y los estados existentes sin crear una segunda definición de cuándo el contenido es público.

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/admin_api.py` (`def publish`) — Publicar ahora establece estado y fecha de publicación.
- `backend/blog/admin_api.py` (`def draft`) — Volver a borrador retira el artículo del sitio público.
- `backend/blog/admin_api.py` (`def schedule_daily`) — La selección se distribuye un artículo por día sin solaparse.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Publicar ahora fija una fecha pública | staff | — | — | published |
| Pasar a borrador retira el artículo | staff | — | — | draft |

**Cobertura exigida:** api

- `backend/blog/tests/test_admin_api.py`

### BLOG-004 — El listado público entrega como mucho 60 posts por petición

**Estado:** ✅ Implementada

`?limit=` se recorta a 60 y cualquier valor inválido cae al valor por defecto de 24.

> **Por qué:** El endpoint es anónimo y cacheado por combinación de parámetros. Sin techo, un `?limit=100000` serializa el archivo entero y además ensucia Redis con una entrada enorme por cada valor que alguien pruebe. El recorte es silencioso a propósito: quien pide de más recibe una página válida, no un 400 que obligue a manejar el error en el frontend.

**Backend**

- Endpoint: `GET /api/blog/posts/`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/views.py:36-37` (`MAX_LIMIT`)
- `backend/blog/views.py:62-63` (`limit = max(1, min(limit, MAX_LIMIT))`)

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un limit desmedido se recorta al máximo | — | `blog_posts`=3 | — | `max_results`=60 |
| Un limit no numérico no rompe la respuesta | — | `blog_posts`=3 | — | `http_status`=200 |

**Cobertura exigida:** api

- `backend/blog/tests/test_blog_api.py`

### BLOG-005 — Publicar avisa a los buscadores e invalida el caché del frontend

**Estado:** ✅ Implementada

Cuando un post pasa a ser público, se encola un ping de IndexNow con su URL y una revalidación de las etiquetas `blog` y `blog-<slug>` en Next.js.

> **Por qué:** Un artículo que nadie recrawlea durante días no posiciona: la ventaja de IndexNow es que Bing —y con él ChatGPT y Copilot— lo ve el mismo minuto. Y sin invalidar las etiquetas, `/blog` seguiría sirviendo la lista vieja hasta agotar su ventana de ISR, así que el post existiría pero no habría forma de llegar a él navegando.
Programar para dentro de un mes no dispara nada: la señal comprueba `is_public` antes, porque avisar a un buscador de una URL que aún devuelve 404 gasta presupuesto de rastreo y enseña un error.

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/signals.py:39-50` (`def post_saved`)
- `backend/blog/tasks.py:24-52` (`def publish_scheduled_posts`)

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Guardar un post ya público avisa a IndexNow | — | `blog_post_published_days_ago`=1 | — | notificado |
| Guardar un post programado a futuro no avisa a nadie | — | `blog_post_published_in_days`=30 | — | silencio |

**Cobertura exigida:** unit

- `backend/blog/tests/test_scheduling.py`

### BLOG-006 — Las guías migradas conservan su URL mediante redirección permanente

**Estado:** ✅ Implementada

`/guias` y `/guias/<slug>` responden con un 308 hacia `/blog` y `/blog/<slug>`, y los slugs de las siete guías no cambiaron.

> **Por qué:** Las guías llevaban meses indexadas. Moverlas sin redirección tira a la basura el poco historial que tiene un dominio joven y deja siete 404 en Search Console; duplicar el contenido en las dos rutas es peor todavía, porque obliga a Google a elegir canónica y reparte las señales entre dos URLs.
La redirección es permanente (308 en Next.js, equivalente a un 301 que además preserva el método) para que el traspaso de autoridad sea inequívoco, y el contenido vive en un solo sitio.

**Frontend**

- Ruta: `/guias`
- Ruta: `/guias/[slug]`
- Ruta: `/blog`
- Ruta: `/blog/[slug]`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `frontend/next.config.js` (`async redirects`) — Redirige la sección entera y cada guía a su post equivalente.
- `backend/blog/seed/guides.json` (`como-comprar-una-propiedad-en-ecuador`) — El seed conserva los slugs originales; el texto se exportó de `lib/guias.ts` sin reescribirlo.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| La guía de compra responde en su nueva URL | — | — | — | /blog/como-comprar-una-propiedad-en-ecuador |
| La URL antigua redirige de forma permanente | — | — | — | 308 |

### BLOG-007 — Programar un lote no pisa lo que ya está en el calendario

**Estado:** ✅ Implementada

La acción «uno por día» arranca al día siguiente del último post ya programado, de modo que dos lotes seguidos se encolan en vez de solaparse.

> **Por qué:** El caso real es escribir por tandas: veinte posts un lunes, quince el viernes. Si la acción empezara siempre mañana, la segunda tanda caería encima de la primera y saldrían dos artículos al día durante dos semanas —exactamente el patrón que la programación existe para evitar.

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/admin.py:154-182` (`def schedule_daily`)

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un segundo lote continúa donde terminó el primero | — | `blog_scheduled_posts`=3 | — | sin solapamiento |
| Los posts del lote quedan a un día de distancia entre sí | — | — | — | 1 día |

**Cobertura exigida:** unit

- `backend/blog/tests/test_scheduling.py`

### BLOG-008 — Cada autor con nombre público tiene una página enlazable

**Estado:** ✅ Implementada

Los artículos firmados enlazan a `/blog/autor/<autor>`, donde se muestran la credencial pública y todos sus artículos. El schema Article usa esa misma URL para identificar a la persona y la página publica ProfilePage.

> **Por qué:** Un nombre y un cargo sueltos no permiten comprobar quién responde por el contenido. Una URL estable reúne la firma, la credencial y el historial editorial para personas y buscadores, sin inventar perfiles externos.

**Frontend**

- Ruta: `/blog/autor/[autor]`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/models.py` (`self.author_slug = slugify(self.public_author_name)[:140]`) — El slug se deriva al guardar, así que renombrar al autor mueve su página en vez de dejar la firma y la URL en desacuerdo.
- `backend/blog/views.py` (`queryset.filter(author_slug=author)`) — El listado público filtra por autor; la página no re-deriva nada.
- `frontend/app/blog/[slug]/page.tsx` (`const authorPath = post.author_name`) — La firma del artículo y el `author.url` del schema salen del mismo valor.
- `frontend/app/blog/autor/[autor]/page.tsx` (`export default async function AuthorPage`)
- `frontend/app/sitemap.ts` (``${SITE_URL}/blog/autor/${slug}``) — Si el schema afirma la entidad, el sitemap tiene que ofrecerla a rastreo.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un artículo firmado enlaza a su autor | — | — | — | página de autor con sus artículos |
| Renombrar al autor mueve su página | — | — | — | el slug se recalcula al guardar |
| Un autor inexistente no cae al listado completo | — | — | — | cero resultados |

**Cobertura exigida:** api, unit

- `backend/blog/tests/test_blog_seo.py`

### BLOG-009 — Un artículo de ciudad entrega el precio del m² y el inventario de esa ciudad

**Estado:** ✅ Implementada

Un post con `city` publica, dentro del HTML servido, el precio medio del m² de esa ciudad calculado sobre los anuncios del portal, y enlaza a su catálogo y a su índice de precios. Si la muestra no llega al umbral de promoción, se omiten las cifras y queda solo el enlace al catálogo.

> **Por qué:** El blog captura la consulta informativa («cuánto cuesta la alcabala en Quito»); sin un puente al inventario esa visita se va sin ver una sola propiedad. Y lo que hace citable al artículo no es el consejo, que cualquiera repite, sino el dato que solo tiene este portal: por eso la cifra va en el HTML —los rastreadores de IA no ejecutan JavaScript— y se declara como Dataset junto al artículo.
El umbral es la otra mitad de la regla. Publicar un promedio calculado sobre tres anuncios como si fuera el precio de una ciudad es la forma más rápida de que te citen una vez y no te vuelvan a creer.

**Frontend**

- Ruta: `/blog/[slug]`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `frontend/components/blog/CityPriceBlock.tsx` (`if (listings < MIN_LISTINGS_FOR_PROMOTION || pricePerM2 <= 0) return null`) — Por debajo del umbral no se publica cifra, solo el enlace al catálogo.
- `frontend/app/blog/[slug]/page.tsx` (`const cityFacts = post.city ? await getCityPriceFacts(post.city) : null`) — El bloque visible y el JSON-LD leen el mismo resultado, así que el schema no puede afirmar una cifra que la página no muestra.
- `backend/blog/models.py` (`city = models.CharField(`) — La ciudad del artículo se elige en el admin; vacía significa alcance nacional.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Un artículo con ciudad y muestra suficiente | — | — | — | precio del m², enlace al catálogo y al índice de precios |
| Un artículo con ciudad y muestra insuficiente | — | — | — | sin cifras, solo el enlace al catálogo |
| Un artículo sin ciudad | — | — | — | no se añade bloque de ciudad |

**Cobertura exigida:** api

- `backend/blog/tests/test_blog_seo.py`

### BLOG-012 — Un artículo programado no declara que se modificó antes de publicarse

**Estado:** ✅ Implementada

`dateModified` —en el schema Article, en Open Graph, en el sitemap y en la línea «Actualizado el…» de la página— es la más tardía entre la fecha de modificación y la de publicación. Nunca una fecha anterior a la publicación.

> **Por qué:** El calendario editorial escribe los artículos días antes de publicarlos (BLOG-001: manda la fecha, no una tarea), así que `updated_at` queda por detrás de un `published_at` futuro. Entregando ambos campos tal cual, ocho de los quince artículos vivos afirmaban haberse modificado antes de existir. Es una contradicción en la señal que más pesa en las respuestas generadas por IA, donde lo reciente se cita mucho más. Un artículo no se modifica antes de publicarse: la fecha posterior es la única respuesta honesta.

**Frontend**

- Ruta: `/blog/[slug]`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `frontend/lib/blog.ts` (`export function articleModifiedAt`) — Única fuente de la fecha de modificación de un artículo.
- `frontend/app/blog/[slug]/page.tsx` (`dateModified: articleModifiedAt(post)`) — El schema, Open Graph y la fecha visible leen el mismo valor.
- `frontend/app/sitemap.ts` (`new Date(articleModifiedAt(post))`) — El `lastmod` del artículo usa la misma regla que su schema (SEO-006).
- `frontend/lib/blog.test.ts` (`describe('articleModifiedAt'`) — Cubre edición real, post programado, fechas iguales y fechas ausentes.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Artículo editado un mes después de publicarse | — | — | — | dateModified es la fecha de edición |
| Artículo escrito el día 11 y programado para el 19 | — | — | — | dateModified es el 19, no el 11 |
| Artículo sin fecha de modificación | — | — | — | dateModified es la fecha de publicación |

**Cobertura exigida:** unit

- `frontend/lib/blog.test.ts`

### BLOG-013 — Un artículo con cifras escritas a mano se revisa cada trimestre

**Estado:** ✅ Implementada

Una tarea semanal lista los artículos publicados que llevan más de noventa días sin tocarse y contienen una cifra escrita en el texto. Solo informa: no reescribe nada.

> **Por qué:** Los bloques que la página renderiza —el precio del m², el ranking vivo— se recalculan solos, pero un número tecleado dentro de un párrafo no, y nada en el sistema sabía qué artículos llevaban uno. Sin esa lista, la corrección depende de que alguien recuerde qué escribió en julio.
Informa y no edita a propósito. Reescribir un párrafo publicado sin que lo lea una persona es la forma más rápida de que un artículo contradiga al bloque de datos que tiene justo debajo.

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/tasks.py` (`def flag_stale_figures`)
- `backend/blog/tasks.py` (`STALE_AFTER_DAYS`)
- `backend/estate_map/settings.py` (`flag-stale-blog-figures`)
- `backend/blog/tests/test_scheduling.py` (`def test_an_old_article_with_typed_figures_is_flagged`)

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Artículo de hace 200 días con "$776" en el cuerpo | — | — | — | aparece en la lista |
| Artículo de hace 200 días sin ninguna cifra | — | — | — | no aparece |
| Artículo revisado esta semana | — | — | — | no aparece |

**Cobertura exigida:** api

- `backend/blog/tests/test_scheduling.py`

### BLOG-014 — Un artículo publicitario se identifica donde se lee

**Estado:** ✅ Implementada

Un post con anunciante lleva un aviso visible antes del título que nombra quién lo paga y si lo paga, la tarjeta del listado lo marca como «Publicidad», y todos sus enlaces salientes salen con `rel="sponsored nofollow"`. El distintivo de la tarjeta **sustituye** a la categoría y nombra al anunciante, y un artículo con anunciante nunca ocupa el hueco de «Lectura destacada». Sin anunciante nada de eso aparece.

> **Por qué:** La regla de Google es que la colocación pagada se declare y sus enlaces se marquen, pero la razón que importa aquí es más estrecha: este portal publica cifras de mercado, y el día que un lector no pueda distinguir lo que el portal midió de lo que alguien compró, las cifras dejan de valer la pena citarse. Por eso el aviso no es una nota al pie ni un color: es una línea que se lee antes del texto. «Del grupo» se dice tal cual —Aents y Geo Propiedades comparten dueños y no media dinero— porque disimularlo sería el mismo fallo en tamaño pequeño.

**Frontend**

- Ruta: `/blog/[slug]`
- Ruta: `/blog/categoria/[slug]`

**Evidencia en el código** (verificada por `tools/specs/validate.py`)

- `backend/blog/models.py` (`def is_sponsored`)
- `backend/blog/models.py` (`SponsorKind`) — Reutiliza el vocabulario de las campañas en vez de inventar otro.
- `backend/blog/serializers.py` (`def get_sponsor`) — La etiqueta viaja con cada representación del artículo, no solo en el admin.
- `frontend/components/blog/SponsoredNotice.tsx` (`export default function SponsoredNotice`)
- `frontend/lib/markdown.tsx` (`sponsored ? 'sponsored noopener noreferrer nofollow'`)
- `frontend/app/blog/page.tsx` (`const editorial = posts.filter((post) => !post.sponsor)`) — «Lectura destacada» es el portal recomendando algo, así que la publicidad no puede ocuparla ni por un is_featured marcado a mano.
- `frontend/components/blog/PostCard.tsx` (`post.sponsor ?`) — El aviso también viaja en la tarjeta, antes de que se abra el artículo.
- `frontend/lib/markdown.test.tsx` (`describe('sponsored links'`) — Un caso comprueba que se marca y otro que un editorial se queda intacto.

**Casos**

| Caso | Rol | Estado previo | Cuerpo | Esperado |
| --- | --- | --- | --- | --- |
| Artículo con anunciante | — | — | — | aviso visible, etiqueta en la tarjeta y rel="sponsored nofollow" |
| Artículo editorial | — | — | — | sin aviso y sin sponsored en los enlaces |
