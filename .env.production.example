# ========================================
# ESTATEMAP - PRODUCCIÓN
# ========================================
# Copiar este archivo a .env.production y configurar con valores reales

# ========================================
# DOMINIO
# ========================================
# Tu dominio sin https:// (ejemplo: estatemap.com)
DOMAIN=tu-dominio.com

# ========================================
# DJANGO SETTINGS
# ========================================
# Generar con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=tu-secret-key-muy-seguro-aqui-cambiar-esto

# Hosts permitidos (separados por coma)
DJANGO_ALLOWED_HOSTS=tu-dominio.com,www.tu-dominio.com

# Orígenes CORS permitidos (separados por coma)
CORS_ALLOWED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com

# ========================================
# BASE DE DATOS (PostgreSQL)
# ========================================
# Nombre de la base de datos
POSTGRES_DB=estatemap

# Usuario de PostgreSQL
POSTGRES_USER=postgres

# Contraseña de PostgreSQL (cambiar por una segura)
DB_PASSWORD=tu-password-seguro-para-postgres

# ========================================
# MINIO (Almacenamiento de imágenes)
# ========================================
# Contraseña del administrador de MinIO (mínimo 8 caracteres)
MINIO_ROOT_PASSWORD=tu-password-seguro-para-minio-minimo-8-caracteres

# ========================================
# NOTAS IMPORTANTES
# ========================================
# 1. NUNCA commitear este archivo con valores reales a Git
# 2. Todas las contraseñas deben ser fuertes y únicas
# 3. El DJANGO_SECRET_KEY debe tener al menos 50 caracteres aleatorios
# 4. El DOMAIN no debe incluir https:// ni trailing slash
# 5. Los valores de ejemplo deben ser reemplazados antes de usar en producción
# 6. Guardar este archivo como .env.production en el servidor

# ========================================
# GENERACIÓN DE CONTRASEÑAS SEGURAS
# ========================================
# Puedes usar estos comandos para generar contraseñas seguras:
#
# Para DJANGO_SECRET_KEY:
#   python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
#
# Para contraseñas generales (32 caracteres):
#   openssl rand -base64 32
#
# Para contraseñas generales (alternativa):
#   cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1
